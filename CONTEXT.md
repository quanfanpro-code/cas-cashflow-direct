# cas-cashflow-direct 当前实现上下文

## 当前目标

本版本把直接法现金流量表复核定位为异常纠偏，而不是全量重新裁决。两个来源、四档质量、十种分数和重要性评价用于授予修改原项目的权限；原项目有效时，系统没有完成修改举证就保持原项目。

## 领域术语

**成本流转链**：外购商品、材料或外部生产服务经过存货入口、成本归集和营业成本的会计路径。它只解释业务性质在科目间的延续，不根据最终成本去向倒推本次现金支付。

**外购入口**：现金业务的完整路径明确显示企业从外部购买商品、材料、委托加工或生产履约服务的位置。材料采购、在途物资和原材料等只有在没有非现金流转或相反属性时才能形成外购证据。

**内部成本流转**：领用、分配、入库、完工、结转、转销、计提、减值、摊销或折旧等不产生本次现金收付的成本移动。

**最终成本去向**：主营业务成本或其他业务成本等利润表归宿。该去向本身不属于现金流项目证据。

## 已确认规则

- 证据来源只有本行摘要和本行完整对方科目路径。
- 原现流项目只用于比较，不属于证据。
- 原项目能够标准化时是默认列报基线。评分、AI和人工构成修改原项目的分级举证链，不构成保留原项目的审批链。
- 原项目有效且未达到整体重要性时，低分、无分、候选不唯一或AI仍不能完成修改举证，最终结果是保持原项目；但达到实际执行重要性且分数不超过50分时，须先按行动表完成一次AI及必要的A/B检查，不能跳过规定程序。
- AI只复核已经成立的修改主张，或帮助原项目为空、无法标准化的业务形成补列结论；AI举证失败不能把有效原项目推给人工。
- 来源质量为0、10、25、45；总分只能是0、10、20、25、35、45、50、55、70、90。
- 70和90必须来自两个独立来源；复述不重复加分；来源冲突没有可用总分。
- 重要性只按单笔金额分层并进入统一行动路由。同类累计金额不形成异常证据、不改变逐笔动作，也不产生可靠组、潜在累计组、整组人工确认或最终完成门禁。
- 每次新运行由客户从45、50、55、70、90中选择有效原项目的自动修改最低证据分，推荐并默认70；五档统一按最终有效总分是否达到所选值判断，选择45时45、50、55、70、90均可修改。存在相反证据或冲突时不形成有效得分，任何档位都不能据此修改。低于本次授权条件只表示没有修改权限，不影响有效原项目继续保留。
- 完整路径每个节点必须是直接识别、继承父属性、中性限定或明确未识别，不得静默丢失；具体法定税种完整路径在候选唯一且无冲突时为45分强证据。
- AI轮次由原项目状态、证据分数、单笔金额档位和强制检查共同决定，不能用“某档固定一次或两次AI”代替两张行动表。达到整体重要性的单笔通常人工决定；唯一例外是原项目为空或无法标准化、90分唯一项目经一次AI确认后自动补列。
- 相同原始来源形成不同项目时，先作为业务事实冲突进入统一行动中心，不允许整组AI编造统一项目；原项目有效且单笔未达到整体重要性时保持原项目，原项目为空或无法标准化时最低金额档进入低金额人工批量，其余金额档人工决定。
- 分类AI只能查看本行原始摘要、完整对方科目路径和有效规则，自行重新解释两个来源，不得查看系统候选池。方向不相容、退款、退回或冲减不另设特殊AI任务。AI不新增证据、不加分、不决定重要性和行动。
- 分类行动表没有串行第二次AI。串行第二次判断只用于分类前的业务组成结构门禁；多个守恒组合未唯一确定时，不做科目语义、评分和分类。
- 每个分类AI任务最多提交三次；第三次仍不合格形成“无有效结果”终态并执行当前分类行动格失败出口。业务组成结构AI连续三次失败则转Agent确认结构，不进入分类行动表。技术失败不算票，最终工作簿不得存在等待AI。
- 代扣个税服务对象不明时，最低两个金额档有有效原项目就保留；原项目缺失时，最低金额档进入低金额人工批量，第二金额档进入不受最低金额限制的人工批量决定。
- 进项税、销项税只有在同凭证、同方向、共用已确认现金来源且基础交易唯一时才随基础交易归类；业务组成拆分不得截断真实价税关系。基础项目待人工时，附属增值税保留独立金额和来源明细，但不重复要求人工选择；基础项目落定项目或明确排除后，税额组成自动跟随同一决定。价税关系缺失或冲突时不得猜测，也不得仅因金额小强制复核。附属税额造成原项目差异时，差异表说明“随唯一基础项目确定”且评分不适用，不把附属跟随计入独立评分自动改判。福利费、往来款和其他分录不得借同凭证另一分录的候选。公司规则必须经用户确认、带版本和适用范围；冲突、停用、替代或拒绝使依赖结果失效。
- 退款、退回和冲减只是摘要或路径中的普通业务关系。只有其原文语义为修改原项目提供正面证据时才参与候选和评分；不得由此建立特殊授权、长期规则、单独AI、人工确认或完成门禁。
- 摘要或完整路径非法时保留原始行和金额，警告并隔离；外部证据或人工决定前不得最终完成。
- 人工确认项目是唯一必填决定；人工处理状态由工作簿公式只根据人工确认项目自动形成，不是第二个人工输入。人工依据、外部资料位置、处理人和处理时间保留但默认隐藏，是否填写由人工自行决定；人工结果不改原证据分数。
- 系统首选项目是“人工确认项目”下拉菜单中的操作选项，不是现金流量表标准项目。已有系统候选时指向该候选；没有新候选但原项目能够标准化时指向原项目这一默认列报基线。确无首选的非法输入例外行仍保留该操作项，但选择后必须提示改选，不能算处理完成。它不得出现在“人工可选标准项目”展示列。
- 真实摘要语义结果是按摘要原文逐项形成的动作、对象、角色、用途、项目、资产、资本化、投资筹资税费、退款冲减、否定、不确定、条件性及准确原文位置记录。固定规则先解释已确认句式；只有语言结构无法解析时，Agent才补充受控语义槽位。候选项目和0、10、25、45分始终由固定程序形成，Agent不得直接返回项目、质量或分数。
- 旧摘要关键词不属于正式摘要语义，也不能在结构化语义缺失或失败时兜底形成候选、质量、分数或动作。统一低置信度、空候选、空分类事实的占位结果不属于真实摘要语义结果，不能作为真实验收输入。
- 有客户正表时，客户金额是自动基线的起点；系统自动改判、自动补列、自动排除和内部划转排除形成“系统自动调整”，客户金额加系统自动调整才是自动基线。等待人工不改变自动基线，人工决定只进入人工调整，最终金额等于自动基线加人工调整。
- 客户正表与业务组成重建之间的差额属于原表与明细勾稽差额，必须单列揭示，不得冒充系统自动调整或静默改变自动基线。
- 摘要中的动作、对象和修饰语必须先完成关系绑定。动作加孤立对象最高为25分；只有唯一候选同时具备独立的用途、属性、业务关系或明确服务对象，且内部无冲突时才能形成45分。
- 外部服务、安装改造、劳务派遣、个人所得税和水电费用必须绑定用途或服务对象：生产检测、委外加工、客户项目履约、管理咨询、自有设备改造、日常维修、工资个税、股权激励个税、分红个税、生产用电和办公用电不得被孤立“服务”“工资”“个税”“电费”覆盖。
- 通用科目词典只识别节点概念；完整一级科目、中间层级、末级明细和限定词共同形成路径候选与质量。单个科目段不得直接决定项目和分数；固定程序无法组合时，Agent只能补受控节点概念和父子关系，不能返回项目、质量或分数。
- 科目路径覆盖情况只在现有`科目语义词典说明.md`中用于规则质量检查，不参与候选、评分、人工路由或完成门禁，也不新增工作表。
- 旧运行目录不可复用；评分、行动表、规则或词典版本不一致时必须新建运行。

完整评分表、两张行动表和强制检查以桌面权威设计 `2026-08-22-cas-cashflow-direct全面重构-design.md` 为准，代码唯一入口见 `src/cashflow_direct/decision_policy.py`。

## 主要模块

- `statement.py`：分别形成业务组成重建值、系统自动调整、自动基线和人工调整，保持三层金额桥不串层。
- `decision_policy.py`：四档质量、十种分数、重要性边界、权限和两张行动表。
- `evidence.py`：把摘要和完整路径转换为两个来源评价，去重并识别冲突。
- `classification.py`：只形成候选和原项目比较状态，再调用统一行动中心。
- `account_dictionary.py`：识别科目节点概念并按完整父子路径形成候选和质量；单个科目段不直接分类。
- `materiality.py`：单笔重要性评价，生成人工批次；不承担同类累计分组或累计确认。
- `ai_review.py`：结构化AI输入校验、系统重新计分和双AI结果处理。
- `component_structure_ai.py`：分类前业务组成结构任务、互盲结果和受限裁决。
- `consistency.py`：只检查真正相同的原始来源，把不一致转为业务事实冲突。
- `pipeline.py`：运行状态、版本、持久化、人工决定和最终门禁。
- `trace_output.py`：只展开实际参与现金流业务组成的现金分配行；同一业务组成分配到多个现金账户时逐行展示且金额守恒，不输出非现金附带分录。
- `workbook_output.py`：诊断材料或最终工作簿生成与结构验收。

## 运行阶段

`preflight → confirm-mapping（如需）→ confirm-account-mapping（如需）→ confirm-notes（如需）→ confirm-cash → supplement-cash（如需）→ classify → import-component-ai / confirm-components（如需）→ import-dictionary（如需）→ classify / import-summary（如需）→ classify → import-ai（如需）→ finalize → 用户在最终工作簿的人工选择区完成最后确认`

`confirm-account-mapping` 是计算前硬门禁。所有一级科目映射完成之前，不生成现金范围候选、业务组成、评分或重要待复核事项。

完整计算顺序为：一级科目映射 → 现金范围确认 → 清理非现金分录 → 构造业务组成 → 应用摘要和完整路径两项结构化语义 → 候选和评分 → 单笔重要性 → 强制检查和行动路由 → 必要的AI复核及系统重算 → 最终工作簿人工确认。

科目语义只扫描实际进入业务组成的完整对方科目路径。兼容入口`scan-accounts`也必须先完成现金分录清理和业务组成筛选，不得把完全不涉及现金的凭证提前送入语义任务。

`finalize`遇到未完成AI时停止。普通待人工事项、能够可靠保留现金腿和金额的非法输入事项及疑似重复事项进入同一最终工作簿，状态为“待完成人工确认”；用户在工作簿中完成选择并用Microsoft Excel重算保存后形成完整结果。工作簿不设置可靠同类组确认Sheet或累计金额完成门禁。映射未确认、现金范围未确认、金额不守恒或勾稽差异不为0时只能形成诊断材料，不得标记“最终可使用”。

## 测试边界

- 第四阶段只使用构造数据和普通回归；不得列出、读取、复制、散列或使用真实验收目录。
- 第五阶段才通过 `CAS_CASHFLOW_REAL_*` 环境参数使用真实文件；真实测试不得按文件名、企业名、金额、行号或特有词写规则。
- 第五阶段验收通用不变量：输入识别、金额守恒、来源分配不重复、来源独立、十种分数、唯一行动、AI成功或失败终态、工作簿公式与留痕以及输入哈希不变。仍有人工事项或勾稽差异时，必须如实停在“待完成人工确认”或诊断状态，不得强行要求或冒充“最终可使用”。
- 第五阶段必须先验证真实摘要语义没有退化为全量空候选和空分类事实，再统计本次自动修改最低分及以上的分数和自动修改。达到本次门槛的证据或自动修改为0时先完成根因分析，不得直接宣告真实验收通过。

## 完成标准

代码、README、SKILL、设计和测试必须只使用统一评分中心和统一行动中心，不得保留第二套计算或决定路径。2026-08-23 14:55形成的真实验收使用了全量低置信度、空候选、空分类事实的占位摘要结果，其614项通过、111项人工决定和8,753,981.55元勾稽差异不得继续作为真实验收完成证据。

上述阻断及后续增值税附属关系、语义补强和45分最低阈值已经关闭。2026-08-24最新隔离运行重新处理14,399条原始分录、6,184个业务组成、5,355项唯一摘要和184条完整路径。摘要全部由固定程序完成，摘要语义Agent任务为0；路径固定规则完整解释122条、部分解释60条、未识别2条，每个路径节点均有明确去向。默认70分下，独立评分自动改判恰好1项车船税；354个增值税组成找到唯一基础业务并自动跟随，其中6条因原项目不同进入差异表；另有1条内部划转差异，共8条差异明细。附属跟随不计为增值税自身评分改判。工作簿恰有十二张表、外部链接0；由于仍有75项人工确认和270.02元现金勾稽差异，状态保持“待完成人工确认”。十万行性能测试未重复执行，真实任务未调用LM Studio或其他模型服务。
