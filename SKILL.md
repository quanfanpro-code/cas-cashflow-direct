---
name: cas-cashflow-direct
description: Use when 需要编制、校验或复核一般企业直接法现金流量表正表，处理复杂Excel序时账、现流明细、现金范围、分类评分和人工复核底稿。
---

# 一般企业直接法现金流量表正表

本 Skill 用于一般企业直接法现金流量表正表的编制、校验、分类复核和底稿输出。正式需求、方案设计和实施计划优先于本操作说明；不得用旧评分、旧路由或历史测试结果覆盖已确认口径。

## 不可越过的原则

1. 原始 Excel 始终只读；处理前后核对文件哈希。
2. 先确认一级科目映射，再确认现金范围并清理非现金分录；形成业务组成并取得摘要和完整路径两项结构化语义后，才能分类、评分、判断重要性和生成待复核事项。
3. 一级科目映射只针对一级科目；全部下级明细继承映射后的一级科目属性，同时保留原完整路径和原账户编码。
4. 现金范围按具体现金账户分别确认；一级科目相同不等于具体账户合并。
5. 原现金流项目只用于与候选比较，不是摘要或路径证据，不加分。
6. 本系统是异常纠偏系统，不是全量重新裁决系统。原项目是默认基线；修改原项目的举证责任在修改方，系统、AI或人工只需证明为什么可以修改，不需要反过来证明为什么可以保留。
7. 候选与最终决定分开。未决定业务不得进入最终正表汇总。
8. 摘要为空或完整对方科目路径非法属于非法输入：保留原始行和现金金额、警告用户、隔离业务、阻止最终完成，不把缺失字段伪装成0分。
9. 达到整体重要性M3的业务通常由用户决定；唯一例外是原项目为空或无法标准化、证据90分且经一次AI确认的唯一项目，可以自动补列。AI仍只能解释原始证据，不能自行决定分数和权限。
10. 真实案例只在需求优先工作流阶段5使用。阶段4不得读取真实案例来写个案规则或抄答案。
11. 支持 `.xlsx`，可只读处理 `.xlsm`；拒绝旧格式 `.xls`，请用户先在Excel中另存为 `.xlsx`。
12. 每次新运行先询问客户选择本次自动修改授权档，只允许45（单强）、50、55、70、90，推荐并默认70。45档要求总分为45、55、70或90且至少一个来源为45分强证据；两个25分形成的纯50分不属于45单强。该选择只控制有效原项目的修改权限，不改变补列、排除、内部划转和强制检查。

## 固定执行顺序

使用 `python scripts/run_cashflow_direct.py`，按状态逐步推进：

`preflight → confirm-mapping（如需）→ confirm-account-mapping（如需）→ confirm-notes（如需）→ confirm-cash → supplement-cash（如需）→ classify → import-component-ai / confirm-components（如需）→ import-dictionary（如需）→ classify / import-summary（仅有未决语言槽位时）→ classify → import-ai（可能多轮）→ finalize → 用户在最终工作簿内完成人工确认`

每一步使用上一步返回的 `run_dir`，不得重新挑文件或用旧运行目录绕过门禁。

开始 `preflight` 前必须先取得客户的自动修改授权档。问法固定为：“本次系统自动修改客户原项目选择哪一档？可选45（单强）、50、55、70、90，推荐并默认70。45单强仅在至少一个来源确为45分强证据且没有冲突时生效，纯50分不算。”客户没有另选时记录70；客户选择其他合法值时按其选择传入 `--automatic-change-threshold`。该选择保存在本次运行中，后续阶段不得擅自改回70，也不得为此调用任何模型服务。

标准流程不要求把人工决定重新导回程序。`confirm-manual` 只保留为已有结构化运行记录的兼容入口，不是普通用户完成工作簿的必经步骤；不得要求用户填写后再向Agent汇报或重新分类。

### 必要输入

- 本期序时账或现流明细；
- 财务报表整体重要性、实际执行重要性、明显微小错报阈值；
- 现金及现金等价物期初、期末余额和汇率影响；
- 校验场景下的客户现金流量表正表；
- 公司特殊核算规则或注意事项，没有则明确记录“无”。

期初、期末和汇率影响优先从客户正表读取；缺失时再询问。严禁倒挤汇率影响。

## 现金分录清理

固定顺序是：一级科目映射 → 现金范围确认 → 清理非现金分录 → 构造业务组成 → 应用摘要和完整路径两项结构化语义 → 形成候选和评分 → 判断单笔重要性 → 强制检查和动作路由。不得先构造业务组成再用分类结果反推现金行，也不得在结构化语义齐全前评分或分流。

现金腿按输入形态识别：

1. 完整序时账中，已确认纳入现金范围的账户行是现金腿；原表是否填写现金流量项目不影响其真实现金金额；
2. 单边现流明细只有在对方科目明确指向已确认现金账户，或已有其他已确认现金锚点时，才构造现金腿；原表现金流量项目和流量金额只能用于比较与异常提示，不能单独制造现金腿；
3. 已确认现金范围内账户与已排除账户之间发生真实转移时，只按现金范围内账户的实际增减形成现金流。

与现金腿有清楚对应关系的非现金分录只用于金额拆分、业务组成和完整对方科目路径证据，不作为另一笔现金流单独输出。其余与现金流无关的附带分录不参与分类、评分和全量分类留痕。若实际输入结构导致现金行无法可靠定位，必须生成现金分录清洗请求，由Agent列明文件、工作表、凭证和来源行，请用户清洗或确认；不得猜测。

## 科目语义阶段

### 完整路径是判断单位

`classify` 先完成现金分录清理和业务组成构造，再只对实际进入业务组成的“已确认标准一级科目＋全部中间层级＋末级明细”完整路径生成语义任务。`scan-accounts` 仅保留为兼容入口，其内部也必须先执行同样的现金分录清理和业务组成筛选；不得提前扫描整张序时账。完全不涉及现金的凭证和无关非现金分录不能进入科目语义任务。同名末级科目位于不同父路径时必须分别判断，例如“管理费用_专业服务费”和“在建工程_专业服务费”不得共用一个结论。

每项必须判断完整路径的一般业务语义、疑似现金流项目、质量档次、判断依据和适用NOTE。

完整路径每个节点必须明确记录为直接识别、继承父属性、中性限定或未识别。任何中间层级或末级明细都不得因为父级已经形成候选而静默消失。税费、职工薪酬、费用成本、票据结算和投资属性按通用词族处理；具体法定税种在路径完整、候选唯一且无冲突时是45分强路径证据。真正未知节点继续如实保留，不为客户名称、金额、凭证号或来源行写特例。

“没有公司特殊规则”只表示不增加企业例外，绝不表示该路径没有通用业务语义。不得把差旅费、停车费、办公费、业务招待费、员工备用金等答成“无企业专属补充语义”后留空。程序会拒绝此类答复。

内置通用词典是基线。普通运行时语义只补齐未知路径，不能标成公司专属层，也不能压过内置通用命中；只有用户明确确认、带NOTE编号且仍有效的企业规则才属于公司专属规则。内置科目段已经能够唯一形成项目时，不得重复送运行时语义解释。

路径原文本身可以作为路径来源依据，理由应明确引用完整路径；外部准则、知识库或NOTE只在实际使用时引用，不能用空泛准则文字替代本行事实。

受限结果示例：

```json
{"task_id":"ACC_…","account":"管理费用_差旅费","node_concepts":[{"level_index":1,"node_text":"差旅费","source_text":"差旅费","concept":"日常经营费用"}],"relations":[{"parent_level_index":0,"child_level_index":1,"relation":"费用性质"}]}
```

科目路径Agent不得返回`item_id`、候选、质量、分数、置信度、重要性或动作。节点概念必须回指原节点中的准确文字，父子关系只能使用任务允许的受控类型；导入后由固定程序重新形成候选和0、10、25、45分。没有可靠补充时提交空`node_concepts`和空`relations`，保留部分解释或未识别，不得用“无公司特殊规则”代替判断。

## 摘要语义阶段

摘要必须先进入`summary_semantics.py`的唯一正式入口。固定程序使用`references/摘要语义规则.json`中的七类词典识别现金动作、业务关系、业务对象、对方角色、凭证语境、信号以及用途和决定性属性，并保存每项事实在摘要原文中的准确起止位置。

固定程序根据动作、对象、关系、角色、用途和属性的组合形成候选，再按“是否足以唯一判断业务”评为0、10、25或45分。单个词、金额、原项目、实际现金方向和Agent自报把握程度都不能直接形成候选或分数。固定规则已经能够解释的摘要，以及信息本来就不足、只能形成低质量或多候选的摘要，均不得为了提分而调用Agent。

只有新表达、修饰归属、多动作绑定、复合分句或表面冲突确实无法由已确认规则解释时，才生成摘要语义Agent任务。该任务只包含摘要原文和允许补充的语言槽位；不得包含金额、原项目、完整对方科目路径、系统候选或其他Agent意见。Agent只能返回受控槽位、枚举值和原文半开区间，不得返回现金流项目、候选、质量、分数或动作。程序验证原文位置后，仍使用同一固定程序重新形成候选和质量。

旧摘要关键词、旧摘要词典和历史Agent结果不得作为正式备用入口。旧运行目录只读；摘要语义版本不一致时必须新建运行。

## 两项证据和十种分数

一笔业务最多只有两个独立业务证据来源：本行摘要完整语义、本行完整对方科目路径完整语义。一级、中间级和末级共同组成一个路径来源；原项目、方向、科目编码、规则编号、AI意见和公司规则都不是第三个来源。

摘要和路径分别按同一套四档质量评价：

| 质量 | 分值 | 含义 |
|---|---:|---|
| 无效 | 0 | 内容属于别行、无业务含义或内部矛盾，不能形成有效候选 |
| 弱 | 10 | 只有部分动作或对象，仍有多个合理候选 |
| 中 | 25 | 已能唯一指向候选，但缺少决定性用途、项目或资本属性 |
| 强 | 45 | 对象、用途或关键属性完整，可唯一排除其他候选且无冲突 |

固定合法总分只有：`0、10、20、25、35、45、50、55、70、90`。

| 两来源组合 | 总分 |
|---|---:|
| 无效＋无效 | 0 |
| 弱＋无效 | 10 |
| 弱＋弱 | 20 |
| 中＋无效 | 25 |
| 中＋弱 | 35 |
| 强＋无效 | 45 |
| 中＋中 | 50 |
| 强＋弱 | 55 |
| 强＋中 | 70 |
| 强＋强 | 90 |

摘要只是复述路径时，两项仍分别记录质量，但合计只算一个来源。两个来源指向不同候选时保留各自质量，合计分为空并标记来源冲突，不平均、不相减、不投票。

摘要和完整路径各自只能在本来源已经形成的候选集合中复核，不能把另一来源或整笔业务的候选借过来。两个来源文字不同但实际复述同一业务事实时仍只算一个来源。

0分只适用于两个非空来源均真实无效的情形。不能形成候选是候选状态，不得冒充原项目冲突；大量正常摘要和完整路径出现0分时必须停止并检查语义入口，不能直接批量推人工。

## 重要性水平

| 层级 | 金额范围 |
|---|---|
| M0 | 绝对金额小于明显微小错报阈值 |
| M1 | 达到明显微小错报阈值但低于实际执行重要性 |
| M2 | 达到实际执行重要性但低于整体重要性 |
| M3 | 达到整体重要性 |

达到阈值立即进入更高层级。重要性评价只按每笔业务自身金额进行；同类事项累计金额不能形成新的异常证据，不能抬高逐笔动作，也不能产生额外人工确认或完成门禁。

## 动作路由

运行级顺序固定为一级科目映射 → 现金范围和现金分录清理 → 业务组成 → 两项结构化语义。进入逐笔判断后，再按输入合法性 → 来源冲突、业务冲突和方向检查 → 原项目状态 → 分数与重要性行动表处理。每次AI复核后重新解释两项来源、重算分数，并重新执行重要性判断、强制检查和行动表。

举证责任固定放在修改方。原项目有效且低于整体重要性时，修改举证没有按行动表完成就保留原项目。本次运行选择的45（单强）、50、55、70或90是有效原项目的统一修改授权档：50、55、70、90按总分是否达到所选档判断；45档只有总分为45、55、70或90且至少一个来源质量为45时才成立，纯50分不成立。M0满足授权才自动修改，M1满足授权才进入一次复核，M2保留既定复核深度；所有复核后的修改主张也必须满足本次授权，否则恢复原项目。默认并推荐70。该授权档不影响原项目为空或无法标准化时的补列规则，也不绕过强制检查。

### 原项目能够标准化并与候选一致

| 分数 | M0 | M1 | M2 | M3 |
|---:|---|---|---|---|
| 0、10、20、25、35、45、50 | 自动保留 | 自动保留 | 一次AI确认能否保留；不能确认时转两个AI互盲，只有一致支持同一新项目且各自达到本次自动修改最低分才修改，否则保留 | 人工 |
| 55、70、90 | 自动保留 | 自动保留 | 自动保留 | 人工 |

### 原项目有效，但候选主张修改

| 分数 | M0 | M1 | M2 | M3 |
|---:|---|---|---|---|
| 未满足本次自动修改授权 | 保留原项目 | 保留原项目 | 按既定复核深度核对，但最终修改主张未满足本次授权就保留 | 人工 |
| 满足本次自动修改授权 | 自动修改 | 一次AI复核，重算仍满足本次授权才修改，否则保留 | 按既定一次或互盲复核深度处理；最终修改主张满足本次授权才修改，否则保留 | 人工 |

### 原项目为空或无法标准化

| 分数 | M0 | M1 | M2 | M3 |
|---:|---|---|---|---|
| 0、10、20、25、35、45、50 | 一次AI；形成有效唯一项目就补列，否则低金额人工批次 | 两个AI互盲；不一致时增加互盲C轮，三份中两份支持同一有效项目就补列，否则人工 | 两个AI互盲；不一致时增加互盲C轮，三份中两份支持同一有效项目就补列，否则人工 | 人工 |
| 55 | 一次AI重算不低于55分且候选唯一时补列；否则进入低金额人工批量 | 一次AI重算不低于55分且候选唯一时补列；否则转A、B，必要时增加C，仍无两票多数时人工 | 两个AI互盲；支持同一项目且每份各自重算不低于55分时补列，否则人工，不增加C | 人工 |
| 70 | 一次AI重算不低于70分且候选唯一时补列；否则进入低金额人工批量，不再调用AI | 一次AI重算不低于70分且候选唯一时补列；否则转A、B，必要时增加C，仍无两票多数时人工 | 一次AI重算不低于70分且候选唯一时补列；否则转A、B，必要时增加C，仍无两票多数时人工 | 人工 |
| 90 | 一次AI重算仍为90分且候选唯一时补列；否则进入低金额人工批量，不再调用AI | 一次AI重算仍为90分且候选唯一时补列；否则转A、B，必要时增加C，仍无两票多数时人工 | 一次AI重算仍为90分且候选唯一时补列；否则转A、B，必要时增加C，仍无两票多数时人工 | 一次AI重算仍为90分且候选唯一时补列；否则人工 |

强制检查优先，但也遵守举证责任：原项目有效时，一般业务事实不足在M0—M2保留原项目；来源冲突在M2先走一次AI确认能否保留，M3人工。原项目为空或无法标准化时按上表尽量由AI补齐。非法输入和现金范围未确认等硬冲突不能用默认保留绕过。现金方向不相容只保留为异常线索，本身不产生复核义务；原项目有效且没有足够正面证据支持修改时保持原项目。

进项税和销项税只有在同凭证、同方向、共用已确认现金来源且只有一个非税基础组成时，才建立附属关系。基础项目落定或明确排除后，税额组成跟随同一决定；基础项目待人工时只让人工在基础行选择一次，税额行保留自己的金额和来源但用受保护公式引用，不设置第二个下拉。关系缺失或冲突时不得猜测，也不得仅因金额小强制复核。附属税额与原项目形成差异时，差异表必须写明“随唯一基础项目确定”且评分不适用，不得冒充税额自身0分自动改判。单独缴纳增值税归支付税费。除进项税、销项税外，福利费、往来款等其他分录不得借同凭证另一分录的候选形成“随交易归类”。

## AI复核闭环

AI只重新解释原始摘要和完整路径，不增加来源数、不额外加分。系统计算总分、重要性和下一步。

- 原项目有效且进入M2复核：继续执行行动表规定的一次或A、B互盲深度；涉及修改时，每份有效结果都必须支持同一新项目并达到本次运行选择的自动修改最低分，其他情况均保留原项目。
- 原项目为空或无法标准化、分数不超过50分：M0一次AI；M1/M2直接A、B互盲。A、B支持同一有效项目即可补列；不一致时再启动完全看不到A、B结果的C轮，三份中两份支持同一有效项目就按多数结果补列，否则人工。
- 原项目为空或无法标准化的55、70、90分按行动表分别执行一次AI或A、B互盲；90分唯一项目即使达到整体重要性，也由一次AI确认后自动补列。
- AI结果不增加来源数、不加分；每轮都只重新解释原始摘要和完整路径。分数下降可以收紧处理，分数上升不能取消已经触发的A、B或C轮。
- 分类行动表没有“串行第二次AI”。串行第二次复核只属于分类前的业务组成结构判断：最低两个金额档第一次结构选择出现低把握、改判或冲突时，才增加第二个结构判断；它不能进入分类评分，也不能替代A、B互盲复核。
- 方向不相容只提示候选和实际现金方向需要核对，不单独触发AI、人工或低金额人工批量。

AI结果使用批次任务规定的结构化字段，分别给出摘要候选、摘要质量、路径候选、路径质量、原文依据、分类事实、独立性、冲突、方向、备选、轮次和复核差异。结构化摘要还必须保留否定、不确定、条件性表达及原文位置；存在不确定或条件性表达时不能评为中、高质量。摘要和路径各自受本来源候选集合限制；第二轮只写“与上一轮一致”等套话不算差异。缺项、引用不存在、候选越界、四档之外质量或伪造NOTE时整条拒绝。

凡填写候选项目，科目路径语义和分类AI复核结果都必须同时提供与该项目相符的 `standard_basis`。泛泛写“根据准则”“参考知识库”，或引用与候选项目不对应的条款，均不得导入。摘要语义Agent不得填写候选项目或准则结论；摘要候选和质量始终由固定程序形成。

退款、退回和冲减作为普通业务关系进入摘要语义。它们可以帮助形成正面证据或方向线索，但不得建立专属AI、人工确认、长期授权或完成门禁。原项目有效且现有正面证据不足以修改时保持原项目。

净额项目必须先取得并确认净额列报所需事实。无法识别服务性质的代扣代缴个人所得税，在最低两个金额档有有效原项目时保留；原项目缺失时，最低金额档进入低金额人工批量，第二金额档进入人工批量决定。第三档由A、B判断服务对象，一致且依据有效后再进入正常行动表；达到整体重要性时人工决定。

`import-ai` 后按返回状态继续：等待后续复核时处理 `AI后续复核请求_*.jsonl`；其中A、B为两个互盲任务，C为第三个独立互盲任务。每个分类AI任务最多提交三次；第三次技术校验仍失败时形成“无有效结果”终态，并立即执行当前分类行动格规定的保持原项目、后续A/B或C、低金额人工批量、人工批量决定或人工决定出口。业务组成结构AI连续三次失败则转Agent确认结构，不套用分类行动表。技术失败不算一票。所有AI任务必须形成成功或失败终态并清空队列后才能 `finalize`；需要人工决定的业务在最终工作簿中完成，不要求先导回程序。

## 人工事项与输出

“重要待复核事项”逐业务组成一行，直接展示日期、凭证号、摘要、完整路径、金额、原项目标准化结果、候选和备选项目。“单笔金额”使用与借方、贷方和现金变化列一致的金额格式；“人工可选标准项目”只展示具体标准项目和“明确排除”，不得混入操作指令。人工确认项目是形成结果的唯一必填决定；每个数据行都有下拉，第一项“采用系统首选项目”，随后才是具体项目和“明确排除”。人工依据、外部资料位置、处理人和处理时间默认隐藏，是否填写由人工自行决定。用户在底稿中完成后不需要向Agent汇报。

全量分类留痕只保留实际参与现金流业务组成的现金行。同一业务组成分配到多个现金账户时按现金分配行展开，合计必须等于该业务组成现金变化；不涉及现金流的附带分录不得输出。等待人工的项目只显示“等待人工复核”，不设置“人工决定”输入列。

混合型现流明细可能同时包含大量带原现金流项目的对方科目行，以及保证金、其他货币资金等已排除账户的完整分录。带原项目的有效行仍按单边现流明细构造业务；已排除且本身具有现金账户性质的行只作为与范围内现金账户真实流入流出的路径证据，不得再次代理成独立现金流。凭证中已有明确范围内现金行时优先使用该现金行。

最终工作簿固定包含十二张工作表：使用说明与状态、现金流量表正表、正表核对报告、重要待复核事项、疑似重复事项、AI复核记录、原表与系统决定差异、现金范围与现金流量表与货币资金变动的勾稽核对、全量分类留痕、科目语义词典、同类检查、输入识别与字段映射。

“原表与系统决定差异”只展示无需人工即可确认、且原项目标准化结果与审定项目确实不同的结果；候选、等待AI和等待人工的事项不得进入。差异原因只说明具体证据得分、自然语言金额档位及满足的自动修改条件，不复述表内已有的原项目和审定项目。

日期只显示日期，不附带 `T00:00:00`。来源定位、映射过程、评分过程、AI过程、内部编号和版本字段保留但默认隐藏。人工填写区通过工作簿公式形成最终结果，不使用VBA，不要求重新运行程序。

AI未完成时不得生成最终工作簿。AI已经完成但仍有人工事项时，可以生成“待完成人工确认”的唯一工作簿；用户完成黄色或蓝色区域并用Microsoft Excel重算保存后，公式同步形成完整结果。非法输入、映射未确认或勾稽差异不为0时，只能标记“诊断材料，不可作为最终表”。

Excel最终可使用前必须由Microsoft Excel完整重算并保存；不能只设置自动计算模式或读取旧缓存。

## 失败与恢复

| 状态 | 处理 |
|---|---|
| 输入文件已修改 | 用修改后的文件新建运行，旧结果不得复用 |
| waiting_mapping | 完成全部一级科目映射确认，不得继续分类 |
| 待AI判断业务组成 | 处理 `业务组成AI请求_*.jsonl`；只能在系统列出的守恒组合中选择，不得新增事实或组合 |
| 待确认业务组成 | Agent按系统列出的组合请用户确认；确认前不做科目语义、分类和评分 |
| 待科目语义确认 | 完整回答全部路径任务后导入；不得用“无公司特殊规则”批量留空 |
| 待摘要语义确认 | 只处理固定规则无法解析的语言槽位；Agent不得查看或返回会计判断字段 |
| AI未完成 | 只补齐缺失或无效任务，不重复提交已通过任务 |
| 待AI后续复核 | 按任务标记完成A、B互盲复核；出现C轮时，C不得查看A、B结果 |
| 待人工决定 | 输出唯一最终工作簿供用户直接处理；无需导回程序或重新分类 |
| 勾稽差异不为0 | 保留诊断状态，查现金范围、业务组成、未决定事项和Excel重算，不倒挤 |
