---
name: cas-cashflow-direct
description: 编制或校验一般企业直接法现金流量表正表，理解复杂 Excel 序时账和现流明细，输出正表、核对报告及全量判断留痕。
---

# 一般企业直接法现金流量表正表

当用户要根据序时账或现流明细编制、校验一般企业直接法现金流量表正表时，使用本 Skill。

## 客户引导

按以下最短流程执行，不向客户展示内部五类输入编号：

1. 优先由用户直接告诉 agent 文件位置（中文路径直接支持），用 `preflight --paths`（可多个）传入，不再弹窗；只有用户未提供路径时才弹出 Windows 文件选择窗口（纯后备）。有客户现流正表时，由用户明确告诉 agent 是哪个文件，用 `--statement-path` 传入；程序按内容识别，文件名任意，识别失败会报错停下，绝不自动去文件夹查找。
2. 🔴 开始正式处理前主动询问：**"这家公司有没有特殊的核算规则或注意事项？"**（例如某类业务不按常规科目核算、特殊的摘要习惯）。有则由客户口述（agent 整理成结构化条目）或提供文本文件用 `preflight --notes` 传入；整理出的"公司特殊规则清单"必须经客户确认后用 `confirm-notes` 登记才生效，与准则冲突的规则不采用并说明冲突点。详见"公司特殊规则（B9）"小节。
3. 自动识别编制或校验场景、全部业务工作表、数据区合并单元格、多行表头、字段含义及现流项目所在侧。同一文件可同时识别明细与正表两种角色；自动发现疑似客户正表会提问确认；疑似正表未确认纳入核对时不允许标记为“最终可使用”。
4. 只补问三项重要性、期初期末现金余额与汇率影响（编制场景）和当前确实缺失的资料。
5. 展示现金范围预选，对受限资金和保证金等候选一次确认。🔴 CHECKPOINT：现金范围只确认一次，先与客户核对完全部候选科目和受限资金决定。
6. 程序规则全量扫描并形成唯一系统首选。
7. AI 只处理达到筛选条件的编号化小批次（每批≤25项、独立文件、可累计导入，逐编号验漏项、重复、冲突、非法回答）。
8. 系统自动裁决规则与 AI 的冲突；逐笔判断完成后，对同一来源文件、同一凭证、同一摘要但项目不一致的业务组做整组一致性复核。
9. 一致性逻辑矛盾**一律拦截复核**，不再因金额低于明显微小错报临界值而静默放过；金额只决定复核深度：低于实际执行重要性做一轮复核，达到实际执行重要性再做第二轮裁决，达到整体重要性时要求两轮均为高置信度且逐条项目完全一致。确有不同业务实质的组成允许保留拆分并写明理由。
10. 低置信裁决不得覆盖高证据规则；达到实际执行重要性的重大残余同质事项或未收口业务组送人工，**达到财务报表整体重要性的大额事项无论是否收口都强制人工确认**。
11. 生成十张表的工作簿和 SQLite、JSON、JSONL 自动计算基线。
12. 向客户说明程序全量扫描数、AI 实际复核数、人工批次数、粗勾稽结果和现金流量表与货币资金变动的勾稽核对状态。

不要要求客户判断输入属于哪一种内部类型，也不要要求客户手工填写绝对路径。

只索取当前任务真正需要的资料：

- 编制：本期序时账或现流明细、财务报表整体重要性、实际执行的重要性、明显微小错报临界值，以及期初、期末现金余额和汇率变动影响（无外币填 0，来源需说明）。
- 校验：在编制资料基础上，再选择已有现金流量表正表；期初、期末与汇率影响优先从正表读取，缺失时再向客户补问。
- 编制场景缺期初、期末或汇率数据时，必须在分类前补问并用 supplement-cash 补充，不要求重新选择文件或重做分类；严禁倒挤。

三项重要性通常来自本项目审计工作底稿。若客户尚未提供，先说明需要从财务报表整体重要性取数并按事务所项目组采用的实务比例确定另外两项，不替客户臆造金额。

## 文件边界

- 支持 `.xlsx`，可只读处理 `.xlsm`。
- 不支持 `.xls`；提示客户在 Excel 中“另存为”`.xlsx` 后重新选择。
- 原始文件始终只读，处理前后核对 SHA-256。

## 执行原则

1. 全量扫描输入并恢复统一业务组成，系统必须为每项给出唯一首选分类并留痕。
2. 普通往来款默认归入“收到或支付其他与经营活动有关的现金”，除非摘要或科目证据明确指向投资或筹资。
3. 复核路由按"分层 + 同类累计"执行：小金额弱证据且无异常矛盾的接受系统首选；业务规则冲突（`BUSINESS-RULE-CONFLICT`）不分评分档位，达明显微小错报临界值即送 AI；保留原标签的冲突（`label_kept`）评分 40–69 达明显微小错报临界值即送、评分不足 40 须达实际执行重要性；方向异常等逻辑异常达明显微小错报临界值即送；普通证据不足（评分 <70 或证据弱）须达实际执行重要性才送；以上低于各自门槛的事项按同类型分组（现金方向、原项目、系统项目、对方科目组、摘要模式），组内合计毛额达实际执行重要性时整组逐笔送 AI 并注明"同类 N 笔、累计金额"，仍不足则只在台账留痕；**金额达财务报表整体重要性 → 不送逐笔 AI，强制进人工复核**；单批≤25 项。
4. 系统与 AI 判断一致的事项自动收口；只有自动判断仍未收口、存在真实备选现流项目，且最不利重分类影响达实际执行重要性的事项，才进入“重要待复核事项”，该表允许为零行。重大事项没有真实备选项目时停止生成错误的人工复核行并明确提示。
5. 现金范围只确认一次；内部现金划转排除，受限资金与保证金单独判断。
6. 退款或冲减按原项目负数反映，方向异常本身不等于分类错误；同凭证大额流入流出同挂一侧项目时标记异常送 AI，不自动改分类。
7. 只有对方明确属于现金范围，或扣除有证据的对外收支后剩余现金腿能闭合，才认定内部划转；同凭证真实流入和流出分别保留。
8. 一般企业银行存款利息归入其他经营活动现金；具有投资收益、应收利息、债券或贷款投资证据的利息归入投资活动。同一业务组逻辑矛盾**一律拦截复核**，金额只决定复核深度（小的一轮 AI、大的两轮+人工），不再因低于明显微小错报临界值而静默放过。
9. 业务规则只使用摘要、对方科目和现金收付方向，原现流项目不得参与规则匹配。原标签与标准项目全称一致时也只作为低证据保底；业务证据优先，标签一致不提高证据等级，标签冲突按业务证据强弱进入相应 AI 复核门槛；方向不一致仍按原项目负数或冲减处理。判断理由必须写出实际业务证据和完整项目名称，不得用内部规则代号代替。
9b. **推翻原表门槛**：系统改判"原现流项目"须同时满足：证据打分 **≥70**（典型组合为摘要命中 40 + 对方科目明细层命中 30）、**摘要与对方科目两个独立来源**同时指向同一新项目、**无冲突**且**现金方向兼容**；不足则保留原标签（`label_kept`）并按第 3 条路由送复核。"两个独立来源"仅指摘要、对方科目两类，原标签、现金方向、规则编号、同一科目的不同层级均不计入；仅对方科目一级命中永远不得推翻原表。冲突认定采用分差口径：另一项目总分 ≥40 且与首选分差小于 30（一个完整明细档）才算冲突，分差 ≥30 视为首选有压倒性独立渠道支持、自动收口。**证据打分（70/40 等）是内部业务证据指数**，只用于控制复核深度与改判权限，不代表正确率，不是准则规定，也未做统计校准。
10. 未标项目的多科目凭证不得机械逐科目拆分；原则上按现金净变化形成一个业务组成，只有已分别标注或本金、利息等性质和金额均明确时才拆分。
11. 客户现有正表应扫描全部工作表并兼容本年、上年和本期、上期表头；多张完整候选正表时必须提示歧义。
12. 同一业务组按“同一来源文件、同一凭证、规范化后相同且非空的摘要”识别；只有当前项目不一致的组才复核。不得因发现新摘要就给分类规则追加业务词补丁，也不得把同凭证所有组成机械改成同一项目。
13. 原明细“主表项目名称”与自动判定项目的比较必须先把原项目标准化，再比较标准项目；差异表展示时必须保留原文字，不得用标准化结果覆盖。无法标准化、标准项目不一致及自动判定“不进入正表”的原始明细均应逐行列示。该表只用于展示和复核，不设置人工修改栏，也不参与正表金额、勾稽核对或最终状态计算。

## 最终交付

输出美化的 Excel 工作簿，至少包含正表、核对报告、重要待复核事项、疑似重复事项、AI 复核记录、原表与自动判定差异、现金范围与现金流量表与货币资金变动的勾稽核对、全量分类留痕及输入识别与字段映射。“原表与自动判定差异”逐行保留日期、凭证、摘要、科目、借贷金额、流量金额、原主表项目、对方科目、标准化结果、自动判定及来源定位；源文件没有的可选字段留空，不得臆造。西文和数字使用 Times New Roman，中文由简体中文主题固定为宋体，禁止使用 MS Gothic。除现金流量表正表外，人类使用的项目栏只显示完整项目名称，不显示项目编号；`AI复核记录`和`输入识别与字段映射`默认隐藏，核对报告的支持组成列、重要待复核事项的批次编号与业务组成编号列、全量分类留痕的三列技术字段默认隐藏。人工区初始为空，只允许“认可自动判断”或真实备选项目；**达到财务报表整体重要性的强制人工复核事项例外：可在下拉中改选 22 个标准叶子项目中的任意一个**（完整项目清单写在默认隐藏的技术列，下拉用区域引用，不受 Excel 内联列表长度限制）；直接粘贴无效文字必须保持零调整并阻止最终状态。正表公式和自动计算区应防误改，工作表保护沿用既有设置。人工选择自动反映到正表、核对报告、勾稽核对页和首页状态，不需再次运行 Skill。人工处理后工作簿为最终结果，数据库和逐行记录保留首次 AI、裁决、最终分类、内部划转和原始定位证据。证据强度（高/中/低）、异常、决策来源等人类可读列一律中文大白话；英文技术值只存于默认隐藏的技术列（如 `命中规则(技术)`、`决策来源(技术)` 等），机器留痕保留供复算追溯。

交付时明确区分：程序扫描全部输入；AI 只看被选中的编号化事项；人工只看重大残余批次。不要声称 AI 核对了未送入 AI 请求的明细。

## 执行编排

按固定顺序调用 `python scripts/run_cashflow_direct.py` 的子命令走完整个流程。每一步返回 JSON 决定下一步；`run_dir` 由上一步自动返回，不要重新选择文件。

主流程：`preflight`(`--paths` 直接给路径 或 弹窗；`--notes` 公司特殊规则) → 若 `mapping_question_count>0` 再 `confirm-mapping` → 有 `company_notes_raw` 时 `confirm-notes` 登记公司特殊规则 → `confirm-cash` → `scan-accounts` →（若有待判断科目：AI 逐批作答 → `import-dictionary`，再 `scan-accounts` 确认齐备）→ `classify` → 若 `ai_tasks_missing>0` 依返回状态完成逐笔复核、逐笔裁决、一致性复核和一致性裁决 → `finalize`。`classify` 在科目语义未齐备时会返回"待科目语义确认"，在未确认公司特殊规则时会返回"待确认公司特殊规则"。`status --run-dir` 可随时查看阶段。

- **preflight** `--overall <整体:元> --performance <实际:元> --trivial <微小:元> [--statement-path <客户现流正表文件>]` → 返回 `run_dir`、`mapping_question_count`、`recommended_cash_decisions`、`status`。客户提供现流正表核对时，由用户明确告知正表文件并用 `--statement-path` 传入；程序只对该文件做正表内容识别（文件名任意），识别失败即报错，不自动猜测其余文件。同一文件可同时被读成明细与正表；未指定正表时自动发现的疑似正表会进入 `confirm-mapping` 以 `use`/`ignore` 确认，未确认纳入核对则最终状态不会是可用的。
- **confirm-mapping** `--decisions '<JSON>'`：读 `运行状态.json` 的 `mapping_questions`，字段问题以键 `{file_id}:{sheet}:{role}`（或 `{file_id}:{role}`）传入列字母，如 `{"f1:序时账:摘要":"D"}`；疑似正表问题以键 `{file_id}:statement:{sheet}` 传 `use`/`ignore`，如 `{"f1:statement:现流表":"use"}`。确认 `ignore` 后结果仍保持草稿状态，不会被标记为“最终可使用”。
- **confirm-cash** `--decisions '<JSON>'`：对全部候选科目逐个给 `include`/`exclude`，如 `{"库存现金":"include","保证金-受限":"exclude"}`。🔴 CHECKPOINT：一次确认完。
- **confirm-notes** `--decisions '<JSON数组>'`：登记经用户确认的公司特殊规则清单（见"公司特殊规则"小节）。
- **scan-accounts**：扫描本次运行全部对方科目明细段，生成 `科目语义待判断_第NN批.jsonl`（≤25/批）；无未知明细时标记"科目语义已齐备"。
- **import-dictionary** `--result-path <科目语义AI结果>`：校验并导入企业专属科目语义，生成 `科目语义词典说明.md`，标记齐备。
- **classify** → 返回 `ai_tasks_missing`；>0 时生成 `AI复核请求_第NN批.jsonl`（≤25行/批）。classify 完成后读 `运行状态.json` 的 `rough_reconciliation` 字段，向客户展示粗勾稽是否适用、加总金额、期望值与差异。
- **finalize** → 返回 `workbook_path`、`overall_status`。

### AI 复核闭环

**判断依据（禁止猜测）**：对每个事项的归类必须依据本 skill 内置资料，不得凭经验或常识猜测：

1. 先看系统首选及 `rule_evidence` 引用的 `references/直接法分类规则.json` 确定性规则。
2. 规则不能唯一判断时，查阅 `references/准则原文/企业会计准则第31号——现金流量表.md`，并按 `references/准则原文/来源索引.md` 的使用顺序执行。
3. 需要应用指南、监管案例或相关准则（如第 21 号租赁、《监管规则适用指引——会计类第 1 号》）细节时，查阅 `references/知识库/知识库_现金流量表相关内容汇编.md`。
4. 只有对规则有效性、准则修订或特殊新业务存在疑问时，才查询财政部网站当前有效原文和实施问答；财政部当前有效原文优先于内置知识库。
5. `reason` 必须写明可追查依据，四类居其一：① 准则条款（含"准则"与"第×条"或"第×项"，如"准则第十条第（一）项"）；② 应用指南章节或不短于 4 字的原文引文（如"应用指南第三十二章'取得投资收益收到的现金'"）；③ 知识库具体位置（行号、章节或引文）；④ 公司特殊规则 NOTE 编号（如"依据公司特殊规则：NOTE-01"）。**该门禁强制生效**：依据不合格的作答在 `import-ai` 时直接判为无效并列入 `invalid_ids`，必须补写合格依据后重新导入；只写"根据准则""综合判断""摘要支持""根据业务实质"等空泛理由一律拒收，不得作为最终判断理由。

1. 逐批读 `AI复核请求_第NN批.jsonl`，每行一任务（`task_id`/`component_id`/`context`/`original_item`/`system_item_id`/`rule_evidence`）。
2. 对每个 `task_id` 依 `context` 与 `original_item` 判断归入哪个标准项目，逐行输出结果文件：
   ```json
   {"task_id":"AI-…","component_id":"…","item_id":"CFO-03","reason":"判断理由","confidence":"high"}
   ```
   `item_id` 必须是 `references/一般企业正表项目.json` 中 `is_leaf==true` 的 22 个编号（CFO-01…CFF-06）；`confidence` 取 `high`/`medium`/`low`，`reason` 非空。
3. `import-ai --result-path <结果文件>` 一次性导入。
4. 看 `status`：`AI 待裁决`→写逐笔裁决结果（格式同上）再 `import-ai`；`AI 待一致性复核`→读取 `一致性复核请求_第NN批.jsonl`，按 `group_id` 为 `assignments` 中的每个 `component_id` 指定项目，并填写整组理由和置信度；`AI 待一致性裁决`→读取 `一致性裁决请求_第NN批.jsonl`，按同样格式完成第二轮；`AI 未完成`→补齐 `missing_ids` 后重导；只有 `AI 已完成`才进入 `finalize`。
   ```json
   {"task_id":"CAI_…","group_id":"CGR_…","assignments":{"CMP_…":"CFI-06","CMP_…":"CFI-06"},"reason":"整组判断理由","confidence":"high"}
   ```
5. 科目语义批次（`scan-accounts` 生成）：每行 `{"task_id","account","sample_contexts"}`，作答按 `{"task_id","account","semantic","item_id","confidence","basis"}` 用 `import-dictionary` 导入；`item_id` 可为空串（表示"已识别但不指向特定项目"），`confidence` ∈ {high,medium,low}，`basis` 非空。

缺期初、期末或汇率时，用 `supplement-cash --run-dir <run_dir> --opening <期初:元> --closing <期末:元> --fx <汇率:元,无则0> --source-note <来源说明>` 补充后重新 `finalize`。严禁用差额倒挤汇率影响。

## 公司特殊规则（B9）

- **两种输入**：① 对话口述，由 agent 整理成结构化条目（`{"note_id","内容","涉及科目或词","建议处理","依据"}`）后用 `confirm-notes` 直接登记，无需任何文本文件；② `preflight --notes <文本文件>` 直接读入原文。无论哪种方式，**整理出的清单必须先向客户展示确认**（`confirm-notes`）后才生效。
- **状态字段**：每条规则带 `状态` 字段，取值"采用"或"冲突未采用"（缺省补"采用"）；`note_id` 缺省时自动按 `NOTE-01`、`NOTE-02`…编号。只有"采用"状态的规则进入语义词典与 AI 复核上下文；"冲突未采用"仅写入说明文档，不生效。
- **生效**：作为企业专属层注入——对口科目语义并入语义词典（权重同专属层），并作为 AI 复核与词典生成的参考上下文。被"采用"规则的涉及词命中的科目段，即使通用词典已认识，也会强制生成专属判断任务，防止通用语义截胡公司特殊规则。
- **留痕**：每笔用到某条特殊规则的判断，原因中须写明"依据公司特殊规则：NOTE-xx"；`科目语义词典说明.md` 附"公司特殊规则"一节，分"已采用"与"冲突未采用（仅说明，不生效）"两组逐条列出。
- **红线**：与准则/知识库冲突的规则不采用，向客户说明冲突点后列"冲突未采用"清单。有 `company_notes_raw` 而未确认时，`scan-accounts`/`classify` 会停在"待确认公司特殊规则"。

## 失败与恢复

| 触发条件 | 处理动作 |
|---------|---------|
| message 为“输入文件已被修改” | 请客户用修改后文件新建一次运行 |
| `confirm-mapping` 报“缺少 XXX” | 补传该字段 decisions 后重试 |
| `import-ai` 报“与已导入结果冲突” | 澄清后重导，勿重复提交 |
| `finalize` 报“AI 尚未逐编号完成” | 回 AI 复核闭环补齐漏答 |
| `finalize` 报“客户现有正表仍有无法映射的项目” | 请客户补标准项目名后重建 |
