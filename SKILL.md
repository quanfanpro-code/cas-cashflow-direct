---
name: cas-cashflow-direct
description: 编制或校验一般企业直接法现金流量表正表，理解复杂 Excel 序时账和现流明细，输出正表、核对报告及全量判断留痕。
---

# 一般企业直接法现金流量表正表

当用户要根据序时账或现流明细编制、校验一般企业直接法现金流量表正表时，使用本 Skill。

## 客户引导

按以下最短流程执行，不向客户展示内部五类输入编号：

1. 弹出 Windows 文件选择窗口，一次选择本期序时账、现流明细和可选的客户现有正表。
2. 自动识别编制或校验场景、全部业务工作表、数据区合并单元格、多行表头、字段含义以及现流项目所在侧。
3. 只补问三项重要性和当前计算确实缺失的资料。
4. 展示现金范围预选，对受限资金和保证金等候选一次确认。
5. 程序规则全量扫描并形成唯一系统首选。
6. AI 只处理达到选择条件的编号化小批次，每批实际写成独立文件且不超过 25 项；各批结果累计导入，逐编号验证漏项、重复、冲突和非法回答，不要求重复提交已完成编号。
7. 系统自动裁决规则与 AI 的冲突；低置信裁决不得覆盖高证据规则，只把达到实际执行重要性的重大残余同质事项送人工。
8. 生成九张表的工作簿和 SQLite、JSON、JSONL 自动计算基线。
9. 向客户说明程序全量扫描数、AI 实际复核数、人工批次数和现金调节状态。

不要要求客户判断输入属于哪一种内部类型，也不要要求客户手工填写绝对路径。

只索取当前任务真正需要的资料：

- 编制：本期序时账或现流明细，以及财务报表整体重要性、实际执行的重要性、明显微小错报临界值。
- 校验：在编制资料基础上，再选择已有现金流量表正表。
- 只有现金及现金等价物调节无法完成时，才补充期初期末余额或科目余额表。
- 只有汇率变动影响无法从现有资料可靠取得时，才补充该金额，严禁倒挤。
- 缺少期初、期末或汇率数据时，在原运行中补充三个金额和来源说明，不要求重新选择文件或重做分类。

三项重要性通常来自本项目审计工作底稿。若客户尚未提供，先说明需要从财务报表整体重要性取数并按事务所项目组采用的实务比例确定另外两项，不替客户臆造金额。

## 文件边界

- 支持 `.xlsx`，并可只读处理 `.xlsm`。
- 不支持 `.xls`；提示客户在 Excel 中“另存为”`.xlsx` 后重新选择。
- 原始文件始终只读，处理前后核对 SHA-256。

## 执行原则

1. 全量扫描输入并恢复统一业务组成，系统必须为每项给出唯一首选分类并留痕。
2. 普通往来款默认归入“收到或支付其他与经营活动有关的现金”，除非摘要或科目证据明确指向投资或筹资活动。
3. 小于明显微小错报临界值的事项接受系统首选，不送 AI；达到阈值的模糊或被规则选中的异常事项才分批复核，单批不超过 25 项。
4. 只有自动判断仍未收口，且最不利重分类影响达到实际执行的重要性的事项，才进入“重要待复核事项”。该表允许为零行。
5. 现金范围只确认一次；内部现金划转排除，受限资金与保证金单独判断。
6. 退款或冲减按原项目负数反映，方向异常本身不等于分类错误。
7. 只有对方明确属于现金范围，或扣除有证据的对外收支后剩余现金腿能够闭合，才能认定内部划转；同凭证真实流入和流出必须分别保留。
8. 一般企业银行存款利息归入其他经营活动现金；具有投资收益、应收利息、债券或贷款投资证据的利息归入投资活动。
9. 原现流项目与 22 个一般企业标准明细项目全称一致时，标准标签优先于关键词规则；方向不一致仍按原项目负数或冲减处理。
10. 未标项目的多科目凭证不得机械逐科目拆分；原则上按现金净变化形成一个业务组成，只有已分别标注或本金、利息等性质和金额均明确时才拆分。
11. 客户现有正表应扫描全部工作表并兼容本年、上年和本期、上期表头；存在多张完整候选正表时必须提示歧义。

## 最终交付

输出美化的 Excel 工作簿，至少包含正表、核对报告、重要待复核事项、疑似重复事项、AI 复核记录、现金调节、全量分类留痕以及输入识别与字段映射。人工区初始为空，只允许选择“认可自动判断”或真实备选项目；直接粘贴无效文字必须保持零调整并阻止最终状态。正表公式和自动计算区应防误改。人工选择会自动反映到正表、核对报告、现金调节和首页状态，不要求再次运行 Skill。人工处理后的工作簿是最终结果，数据库和逐行记录文件保留首次 AI、裁决、最终分类、内部划转和原始定位证据。

交付时明确区分：程序扫描了全部输入；AI 只看被选中的编号化事项；人工只看重大残余批次。不要声称 AI 核对了未送入 AI 请求的明细。

运行入口：

```text
python scripts/run_cashflow_direct.py --help
```
