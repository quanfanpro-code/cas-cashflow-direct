# ADR 0011：完整科目路径由关系规则形成候选与质量

状态：已接受。科目通用词典只识别各层节点概念，不再让单个科目段直接携带现金流项目或证据分数；固定程序必须结合已确认一级科目、中间层级、末级明细、限定词和现金方向解释完整路径，再形成候选和0、10、25、45分。固定规则无法组合父子关系时，Agent只补当前任务允许的节点概念及关系，并引用原节点文字；Agent不得返回项目、质量或分数，导入后仍由固定程序重算。这样避免“管理费用_设备款”被设备词越级改成投资活动，也避免用Agent自报把握代替证据质量；代价是旧路径结果不能复用，必须按新版本在新运行中重做。
