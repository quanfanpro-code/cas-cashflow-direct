# 独立复核报告：正表识别与明细取数修复（阶段5 复核人独立执行）

日期：2026-08-14
复核人：主会话（应用户要求按 requirement-workflow 阶段5 独立复核，不复用既有审查结论）
既有审查：`C:\Users\27651\.dsh\skills\cas-cashflow-direct\docs\reviews\2026-08-14-正表识别与明细取数修复-review.md`（实现方自审）

## 一、复核方法

1. 重读阶段5 必读规范（code-reviewer.md、ponytail-review.md、verification-before-completion.md）。
2. 对 16 个改动文件逐个看 git diff 全文（611 行新增 / 118 行删除），逐行核对设计与计划。
3. 本会话新鲜重跑：全量测试、validate_skill、真实文件验收脚本（不接受实现方转述）。

## 二、逐文件复核结论

| 文件 | 结论 | 说明 |
|---|---|---|
| 字段语义词典.json | ✅ | 加"分配金额""现金流量表项""一级科目"，与测试锁死，无误伤词条 |
| components.py | ✅ | `_keyword_direction` 抽出共用，红冲公式与设计写死口径一致；老分支原样保留 |
| normalization.py | ✅ | 小计剔除在合并填充（logical_row）之后、表头重复判断之前，位置正确；双列存在才启用 |
| statement.py | ✅ 有🟢观察 | 内容识别、并列提问、序号列排除、序数前缀、节标题跳过均符合设计；见三-1、三-2 |
| pipeline.py | ✅ | 指定入口三行为（只认指定文件/失败报错/不在输入中报错）齐全；10% 提示接入正确；留痕加"方向依据" |
| cli.py | ✅ | `--statement-path` 可选参数，透传正确 |
| workbook_structure.py | ✅ | open_workbook_robust 只读优先、dimension 损坏才退回普通模式，4 处调用统一 |
| SKILL.md / 使用说明.md | ✅ | 引导文案与程序行为一致（含"识别失败报错停下、绝不自动去文件夹查找"） |
| 6 个测试文件 | ✅ | 新测试均断言真实行为（方向符号、剔除留痕定位、并列提问、序号排除、命中率门槛、歧义提问），无空洞测试 |

## 三、本次独立复核新发现（既有审查未列出）

1. 🟢 **命中率门槛与序数前缀不一致**：`statement.py` 的 50% 命中率统计用的是直接匹配，而正式解析有"去中文序数前缀"的兜底匹配。若客户正表所有项目名都带序数前缀，会出现"解析本来能认、却被命中率门槛挡在门外"的矛盾。真实场景中序数一般只在节标题上（清平实测通过），故不阻塞，记录备查。
2. 🟢 **低命中率被拒时的报错文案不准**：命中率不足时走到"未找到项目列或可用金额列"这句提问，但实情是"找到了、对不上"。用户看到会困惑。极端情况（自定义行超一半的正表）才触发，不阻塞。
3. 🟢 确认既有审查的三条次要观察成立（指定正表不进结构留痕、文本格式金额列未处理、序数前缀仅覆盖单字"一"到"十"）。

## 四、过度工程检查（ponytail-review）

逐个新函数确认都有真实调用方：`_keyword_direction`（2 处）、`flow_direction_source`（留痕）、`subtotal_exclusion_warning`（pipeline）、`open_workbook_robust`（4 处）、`break_dimension`（2 个测试）。**Lean already. Ship.**

## 五、新鲜验证证据（本对话重跑，非转述）

- 全量回归：`124 passed, 69 subtests passed in 92.36s`（含十万行性能测试）。
- skill 结构校验：`SKILL_VALID=True`。
- 真实文件验收（只读，本会话重跑 `C:\Users\27651\Documents\Code\cas_acceptance\acceptance.py`）：
  - 清平：正表识别成功（35 项）；明细 2895 行 0 错误 0 排除；合计 -127,908,849.44 与正表净增加额分毫不差；逐项对账差异 0 行。
  - 鼎弘：明细 394 行 0 错误；小计剔除 13 行；合计 -14,698,849.94 精确一致。
  - 导出列表：account_name 歧义正确提问（推荐"一级科目"列）；确认后 1000 行 0 错误；219 个组件方向正确。

## 六、结论

实现与需求、设计、计划一致；测试真实有效；新鲜验证全部通过。**复核通过，可进入交付阶段。** 三条🟢观察均为低概率边角，建议记录不立即修。

## 七、边角修复跟进（2026-08-14 当日补记）

交付前用户决定修掉本节三-1、三-2 两个边角，修复按 TDD 完成：

- 三-1（命中率门槛与序数前缀不一致）已修：`statement.py` 抽出 `_match_item_id` 辅助函数，命中率统计与正式解析统一走"直接匹配 + 去中文序数前缀兜底"，门槛改为"零命中直接不认定、匹配率不足 50% 给出准确提示"。新增 `test_hit_rate_gate_uses_ordinal_fallback`（全序数前缀也能认出）、`test_two_char_ordinal_prefix_still_matches`（"十一、"两位序数锁定）两个测试。
- 三-2（报错文案不准）已修：命中率不足时报"项目列与标准现流项目匹配率过低（x/y），未认定为现流正表"；零命中走通用提示、不含"匹配率"字样。新增 `test_low_hit_rate_reports_accurate_reason`、`test_zero_hit_sheet_with_project_column_is_not_statement` 两个测试。
- 三-3（既有审查三条次要观察）维持原结论：低概率边角，不处理。

修复后新鲜验证（本会话重跑）：全量回归 `128 passed, 69 subtests passed in 89.92s`（较复核时 124 项净增 4 项，即上述新测试）；真实文件验收复跑结果不变（清平合计 -127,908,849.44 逐项对账零差异，鼎弘小计剔除 13 行、合计 -14,698,849.94 精确一致）。
