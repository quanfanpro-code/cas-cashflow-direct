# cas-cashflow-direct 全面复核审查报告（2026-08-15）

> 依据 requirement-workflow 阶段 5（验收审查）规范执行，对应执行计划：`C:\Users\27651\Desktop\cas-cashflow-direct全面复核计划_2026-08-15.md`
> 被复核对象：`D:\BaiduSyncdisk\workbuddy skills\cas-cashflow-direct`
> 对照实施计划：`D:\BaiduSyncdisk\workbuddy skills\cas-cashflow-direct\docs\superpowers\plans\2026-08-15-复核能力与双角色修复.md`

## 一、审查范围与依据

- 阶段 5 规范文件已完整读取：`C:\Users\27651\.dsh\skills\requirement-workflow\references\code-reviewer.md`、`ponytail-review.md`、`verification-before-completion.md`，以及 `code-reviewer\reference\` 下的 python、common-bugs-checklist、code-quality-universal、architecture-review-guide、security-review-guide、performance-review-guide。
- 计划第 1 步点名的 `error-handling-principles.md` 实际位于 `C:\Users\27651\.dsh\skills\requirement-workflow\references\code-reviewer\reference\cross-cutting\error-handling-principles.md`（计划少写了一层目录），已按实际路径完整读取。
- requirement-workflow 包完整性校验：`python C:\Users\27651\.dsh\skills\requirement-workflow\scripts\validate_bundle.py` 输出 `BUNDLE_VALID=True`，技能包完整。
- 串行执行，未使用子代理；未做 git 操作；未删除任何文件；客户原始文件只读。

## 二、执行摘要

对照 9 条验收标准逐项取得新鲜证据：**9 条全部通过**。审查中发现并修复 1 个阻断级（🔴）代码缺陷：非现金事项判定把“票据贴现、票据到期收款”等 52 项、合计 **160,201,544.21 元**的真实现金流误判为非现金并排除出正表；按 TDD 修复并新增 2 个回归测试后，全部验证重新通过。README 的旧表述与过时数字、SKILL.md 的疑似正表确认键说明已同步修正。

## 三、9 条验收标准逐条结论

### 验收标准 1：全量测试全绿 + 新增用例全绿

**通过。** 修复前新鲜结果为 141 passed；修复并新增 2 个回归测试后最终结果：

```text
143 passed, 74 subtests passed in 5.62s
PYTEST_EXIT=0
```

命令：`PYTHONIOENCODING=utf-8 PYTHONPATH="src;." python -m pytest tests/ -q --ignore=tests/test_100k_performance.py`
基线 103 + 本轮新增 40 = 143，≥ 基线要求；README 所称“141 项”已同步更新为 143 项。

### 验收标准 2：validate_skill.py 输出 SKILL_VALID

**通过。**

```text
SKILL_VALID=True
VALIDATE_EXIT=0
```

### 验收标准 3：真实文件双场景 preflight

**通过。** 输入文件 `C:\Users\27651\Desktop\现金流量-清平.xlsx`，重要性：整体 100,000,000 元、实际执行 75,000,000 元、明显微小 5,000,000 元。

- 场景 A（不带 `--statement-path`，运行目录 `D:\BaiduSyncdisk\workbuddy skills\cas-cashflow-direct\运行验证_20260815\运行_20260815_145854_1b136f95`）：
  - 明细条数 `source_entry_count = 2895`；
  - `mapping_question_count = 1`，疑似正表候选 `[现金流量-清平.xlsx / 现流表]`；
  - 提问文案：“检测到疑似正表工作表《现流表》，请确认是否作为客户现有正表核对”。
- 场景 B（带 `--statement-path` 指向本文件，运行目录 `...\运行_20260815_145855_334e4a5d`）：
  - 明细条数 `source_entry_count = 2895`（明细未丢）；
  - `mapping_question_count = 0`；
  - `existing_statement_path = C:\Users\27651\Desktop\现金流量-清平.xlsx`（正表已识别）。

### 验收标准 4：classify 回归

**通过。** 修复后新鲜运行结果（运行目录同上场景 B）：

```text
component_count=2895
matched_rule_id 计数: {'EXACT-STANDARD-LABEL': 2436, 'LABEL-RULE-CONFLICT': 404, 'EXCLUDED': 55}
EXACT-STANDARD-LABEL < 2895: True
ai_tasks_missing: 6
```

- `EXACT-STANDARD-LABEL` 2436 条 < 2895 条，标签不再全部免审。
- AI 批次 6 项，包含 1623 万税收滞纳金：金额 `-16,236,343.08`、证据 `LABEL-RULE-CONFLICT：规则CFO-07-PENALTY高证据指向CFO-07`。
- 其余计划清单事项均进入 404 条冲突留痕（金额均低于明显微小临界值 500 万元，按原则 3 不送 AI，符合设计）：长期待摊费用 4 条、在建工程 3 条、结构性存款利息 3 条、电费基金 4 条、车船税 9 条、滞纳金 10 条。
- 记 231 大额票据背书凭证整体排除：金额 `-58,972,968.30`，anomalies=`['non_cash','netting_suspect']`，`excluded=True`。
- 审查修复项：票据贴现、票据到期收款 52 项真实现金流已不再被误排（修复前全部在 EXCLUDED 内）。

### 验收标准 5：工作簿全量分类留痕人机分层

**通过。** 打开修复后 finalize 输出的 `D:\BaiduSyncdisk\workbuddy skills\cas-cashflow-direct\运行验证_20260815\运行_20260815_145855_334e4a5d\现金流量表正表及复核底稿.xlsx`：

- 留痕共 17 列，表头顺序为：记录类型、摘要、现金变化、原现流项目、对方科目、系统项目、判断理由、证据强度、异常、方向依据、来源文件、来源工作表、来源单元格、决策来源(技术)、命中规则(技术)、业务组成编号(技术)、来源占用键(技术)。
- 系统项目列为“中文名称（编号）”格式，样例：`支付其他与经营活动有关的现金（CFO-07）`。
- 四个技术列位于最右侧第 14-17 列，表头均带“(技术)”。

### 验收标准 6：桌面错分标注交付物

**通过（附计划近似值差异说明）。** `C:\Users\27651\Desktop\现流表明细_错分标注.xlsx`：

- “明细”页共 2896 行（含表头）= 2895 条数据行、17 列，右侧新增“是否疑似错分、建议调整至、理由、涉及凭证”4 列。
- “错分汇总”页 14 类 + 表头。各类笔数与金额已用源文件 `C:\Users\27651\Desktop\现流表明细.xlsx` 独立复算，与导出脚本规则一致。
- 与计划规则表中**近似描述**的差异（交付物无误，计划文字本身是约数）：车船税实际 14 行、15,041.40 元（计划写 6 行约 9,987.30）；矿业电费实际 9 行、3,439,438.21 元（计划写“约 443 万”）；资本性支出实际 9 行、1,183,288.09 元（计划写“约 124 万”）。
- 未重跑导出脚本，未覆盖已交付文件。

### 验收标准 7：复盘文档已更正且有备份

**通过。** `C:\Users\27651\Desktop\现金流量表核对_问题复盘.md` 与备份 `C:\Users\27651\Documents\Code\Backup\桌面复盘_20260815\现金流量表核对_问题复盘.md` 的逐行差异确认：“责任划分”已更正为工具双角色缺陷、操作者未传参数只是表象；“教训”已改为“修复前需先把正表页另存为独立文件再指定”，并补充修复后的新行为说明。

### 验收标准 8：客户原始文件 SHA-256 前后一致

**通过。** 本轮全部 preflight/classify/finalize 前后多次计算：

```text
现金流量-清平.xlsx  a4e710b0d0e5ab672c003c1adbde34accc4cdb19309ed25119d3ccdee7700c5b（前后一致）
现流表明细.xlsx      ce18d42b8068f4e3ce778fb61bc22132d4f2c91bf61c1432ac548a770b1b6138（前后一致）
```

### 验收标准 9：备份目录存在且时间戳早于任何修改

**通过。** 最早备份 `C:\Users\27651\Documents\Code\Backup\cas-cashflow-direct_20260815_095425` 创建于 `2026-08-15 09:54:25`；计划内改动文件最早修改时间 `2026-08-15 11:12:34`，无任何改动早于备份。本次复核动手前另备份至 `C:\Users\27651\Documents\Code\Backup\cas-cashflow-direct_20260815_145345`。

## 四、代码审查问题清单（按严重度）

### 🔴 阻断级（已修复）

- **C-1 非现金判定误排真实现金流**（`D:\BaiduSyncdisk\workbuddy skills\cas-cashflow-direct\src\cashflow_direct\components.py`，`_voucher_non_cash_marks`）：
  - 原实现只要凭证含“应收票据/应付票据”且本行科目不是现金，就标记 `non_cash`，既不要求“票据腿与往来腿互抵”，也不看对方科目是否货币资金。
  - 后果：真实客户文件里 52 项票据贴现、票据到期收款（合计 160,201,544.21 元）被排除出正表。
  - 修复：判定改为“有票据腿 + 有往来腿 + 本行科目与对方科目均无货币资金”才标 `non_cash`；并补充中文注释。
  - TDD 证据：新增测试 `test_note_receipt_with_cash_counterpart_stays_real_cash`、`test_note_only_voucher_with_flow_label_is_not_auto_non_cash`，先确认红灯（`'non_cash' unexpectedly found`），修复后组件测试 22 passed、全量 143 passed，且真实文件回归中该 52 项全部保留、记 231 大额凭证仍整体排除。

### 🟡 重要（已处置或记录）

- **D-1 README 与新行为矛盾**（`D:\BaiduSyncdisk\workbuddy skills\cas-cashflow-direct\README.md`）：第 75 行“其余文件一律只按明细读取”、第 151 行“标准标签优先于摘要关键词”、第 179 行状态文案缺“草稿：存在未核对的疑似正表”、第 313/329/349 行测试数与实测数过期。**已全部修正**，详见第七节。
- **D-2 SKILL.md 疑似正表确认键缺失**（`D:\BaiduSyncdisk\workbuddy skills\cas-cashflow-direct\SKILL.md`）：实现使用 `{file_id}:statement:{sheet}`，文档只写了字段映射键格式，执行代理可能构造错误键。**已补充** `use`/`ignore` 说明与示例。
- **P-1 preflight 每次全量读两遍输入**（`src\cashflow_direct\statement.py` `detect_statement_sheets`）：每个文件先整表读取一次做正表识别，再由 `scan_workbook` 整表读取一次做明细识别。10 万行性能测试新鲜结果 `1 passed in 44.96s` 通过，当前规模可接受；超过 README 声明的 10 万行边界时应重新评估。未改代码。
- **O-1 计划外小改动 validation.py**：`C:\...\src\cashflow_direct\validation.py` 在 13:31 被改动（不在实施计划 Files 清单）：`decision_ids` 改为包含 `excluded` 决策。审查结论：该改动是 Task 2 非现金排除落地的必要配套（否则每个 non_cash 业务组成都会因“未取得唯一分类”触发不变量失败），逻辑正确、有全量测试保护，作为计划外但必要的配套改动记录。
- **O-2 三个文件被删除**：`成果卡片.html`、`成果卡片.png`、`docs\superpowers\plans\2026-08-14-正表识别与明细取数修复.md` 不在当前目录，但完整保存在各份备份中。**用户已确认是自己有意删除**，不构成缺陷，未做恢复。

### 🟢 建议

- **G-1** `non_cash` 的“计提”规则要求凭证每一行摘要都含“计提”才排除，属于宁漏勿错；但真实文件中“计提聚佳管理费、社保、工资”两行被整体排除，而错分清单第 13 类建议人工核实是否实际付款，两处口径存在张力。建议后续在计提规则中增加现金腿证据检查，避免把实际付款当计提排除。本轮按计划不改。
- **G-2** `netting_suspect` 未设“大额”金额门槛（计划原文为“大额流入流出”），达到明显微小临界值的同向标签凭证即送 AI，可能增加小金额复核量。当前 AI 批次仅 6 项，影响可控。
- **G-3** 旧审查文档仍引用已删除的 `docs\superpowers\plans\2026-08-14-正表识别与明细取数修复.md`（用户已确认删除），引用悬空但不影响运行。
- **G-4** 本轮编辑工具会去掉 UTF-8 BOM，已逐一恢复（components.py、README.md、SKILL.md）；后续编辑含中文文件后需复核 BOM，避免 `SKILL_VALID=False`。

## 五、ponytail-review 过度工程审查

按 ponytail-review 规范对本次改动范围单独审查：

- `statement.py:L314` `detect_statement_sheets`：逐表识别接口是双角色读取的必要能力，不是可删的抽象。
- `pipeline.py` `statement_candidates` / `statement_confirmations`：持久化确认状态是跨阶段续跑所需，不是冗余状态。
- `components.py` `_voucher_non_cash_marks` 与 `_annotate_voucher_components` 职责清晰、无复制变种；修复后注释解释了判定边界。
- 未发现重造标准库、未使用依赖、单实现抽象层或死配置。

结论：`Lean already. Ship.`

## 六、测试质量评价

- 计划要求的新用例全部真实存在且验证行为：`test_statement.py` 的 `detect_statement_sheets` 逐表结果、`test_pipeline.py` 的同文件双场景端到端与未确认封锁、`test_classification.py` 的冲突三分支、`test_ai_and_materiality.py` 的冲突送 AI 与 trivial 门槛、`test_components.py` 的计提/票据背书 non_cash、`test_workbook_output.py` 的列序与技术列表头。
- 断言针对用户可见行为（计数、异常标记、工作簿表头、状态），不是 mock 调用或实现细节。
- 正常路径、边界（trivial 临界、多批 25 项上限、多正表歧义）与错误路径（指定正表失败、输入被改、无效粘贴）均有覆盖。
- 本次审查新增 2 个票据真实现金流回归测试，把已发现的缺陷锁定为可执行证据。
- 未引入独立 reviewer（用户未授权），未读取 requesting-code-review.md。

## 七、文档同步修正明细

`D:\BaiduSyncdisk\workbuddy skills\cas-cashflow-direct\README.md`：

1. 第 75 行：改为“指定正表只认该文件内容、其余文件先读明细再自动识别疑似正表；疑似正表未确认前不标最终可使用”。
2. 第 151 行：改为“标签仅作平局兜底、规则独立复核、高证据冲突送 AI（仅受明显微小门槛限制）”。
3. 第 179 行：状态文案补“草稿：存在未核对的疑似正表”。
4. 第 217 行：补系统项目“中文名称加编号”与四个“(技术)”列的说明。
5. 第 313 行：141 项 → 143 项。
6. 更新记录实测数字：`2895 个业务组成中 2436 个按原标签归类、404 个冲突留痕、55 个排除`，并补票据贴现修复说明。
7. 更新记录验证段落：128 → 143 项；补“票据贴现误排已修复并加回归测试后复验通过”。

`D:\BaiduSyncdisk\workbuddy skills\cas-cashflow-direct\SKILL.md`：

8. confirm-mapping 条目补疑似正表确认键 `{file_id}:statement:{sheet}` 与 `use`/`ignore` 取值、`ignore` 后仍为草稿的说明。

## 八、已修复项汇总

| # | 位置 | 修复 |
|---|---|---|
| 1 | `src\cashflow_direct\components.py` | 票据非现金判定改为“票据腿+往来腿互抵且两侧均无货币资金” |
| 2 | `tests\test_components.py` | 新增 2 个回归测试（票据贴现/到期收款不得排除） |
| 3 | `README.md` | 修正 7 处与新行为矛盾或过时的段落 |
| 4 | `SKILL.md` | 补疑似正表确认键格式与示例 |
| 5 | `components.py` / `README.md` / `SKILL.md` | 恢复编辑丢失的 UTF-8 BOM |

修复遵循 TDD：新测试先红（`AssertionError: 'non_cash' unexpectedly found`）→ 最小实现 → 定向测试绿 → 全量 143 passed。修改前已备份至 `C:\Users\27651\Documents\Code\Backup\cas-cashflow-direct_20260815_145345`。

## 九、残余风险

- 计提规则按计划排除“摘要含计提”的凭证，可能与实际付款混淆（G-1），错分清单已提示人工核实，本轮按计划不扩围。
- `detect_statement_sheets` 使 preflight 对每个输入整表读取两遍，10 万行规模实测 44.96 秒通过；超过该规模需先评估（P-1）。
- 本次工作簿留痕核查使用“与系统首选一致的占位 AI 应答”完成 finalize，仅用于验证工作簿布局，不代表真实 AI 复核结论；真实客户复核必须使用实际 AI 应答。
- 本轮验证运行的现金调节状态为“草稿：现金调节未完成或存在差异”，原因是验证时未补充期初/期末余额，属于验证场景设定，不是缺陷。

## 十、关键证据位置

- 新鲜双场景运行：`D:\BaiduSyncdisk\workbuddy skills\cas-cashflow-direct\运行验证_20260815\运行_20260815_145854_1b136f95`（场景 A）、`...\运行_20260815_145855_334e4a5d`（场景 B）
- 修复后 finalize 工作簿：`D:\BaiduSyncdisk\workbuddy skills\cas-cashflow-direct\运行验证_20260815\运行_20260815_145855_334e4a5d\现金流量表正表及复核底稿.xlsx`
- 复核前备份：`C:\Users\27651\Documents\Code\Backup\cas-cashflow-direct_20260815_145345`
- 最早实施备份：`C:\Users\27651\Documents\Code\Backup\cas-cashflow-direct_20260815_095425`
