# 复核能力与双角色识别修复 实施计划

> **For agentic workers:** 本计划交给执行 agent 串行执行。步骤用 `- [ ]` 跟踪。计划由复核 agent（甲方）编写，执行 agent 不得扩大范围；完成后由复核 agent 对照"验收标准"逐项复核。

**Goal:** 修复 cas-cashflow-direct 在 2026-08-15 复盘与实测中确认的四个问题：
- ④ 同一文件同时含明细和正表时无法双角色读取（实测三条路径全堵）；
- ② 原现流项目标签命中标准名称即直接放行（EXACT-STANDARD-LABEL 短路），27 条业务规则形同虚设，AI 复核队列恒为空；
- ③ 全量分类留痕 sheet 混杂机器主键列、系统项目只显编号，人类不可读；
- ① 输出一份《现流表明细_错分标注.xlsx》交付物（一次性脚本，不进 skill 源码）。

**实测证据（2026-08-15，可复现）：**
- `现金流量-清平.xlsx`（含"现流表"正表 44 行 + "明细" 2896 行）：不带 `--statement-path` → 明细 2895 条、正表未识别、核对报告为空、`overall_status=最终可使用`；带 `--statement-path` 指向本文件 → 正表识别成功、**明细条数=0**；同文件选两次 → sha256 去重丢弃第二次。
- 上一次运行（桌面 `直接法现流表结果\运行_20260815_084651_b17df922`）：2895 条全部命中 `EXACT-STANDARD-LABEL`，`ai_tasks=0`。1623 万税收滞纳金被照抄进"支付的各项税费"等 9 类错分无一被发现。

**Tech Stack:** Python 3.14、openpyxl、pytest（unittest 风格，fixture 用 `tests/fixture_factory.py` 合成，无 conftest.py）。

## Global Constraints

- **先备份再动手**：修改任何文件前，robocopy 全量备份到 `C:\Users\27651\Documents\Code\Backup\cas-cashflow-direct_<时间戳>\`（排除 .git/__pycache__/.pytest_cache）。禁止在项目文件夹内建 Backup 目录。
- 已有文件一律用 Edit 工具改，禁止 Write 覆盖已有文件；新建文件才能用 Write。
- 禁止 `rm -rf`；禁止 git 提交/推送（是否提交由用户决定）。
- 代码注释与文档一律中文。
- 真实客户文件只读：不得修改 `C:\Users\27651\Desktop\现金流量-清平.xlsx` 与 `C:\Users\27651\Desktop\现流表明细.xlsx`。
- 项目根目录：`D:\BaiduSyncdisk\workbuddy skills\cas-cashflow-direct`（下文称 `<root>`）。
- 测试命令（在 `<root>` 下）：
  `PYTHONIOENCODING=utf-8 PYTHONPATH="src;." python -m pytest tests/ -q --ignore=tests/test_100k_performance.py`
- 基线：2026-08-14 为 103 passed。动手前先跑一次确认基线仍绿，记实际数字。
- 改完结构类内容后运行 `python scripts/validate_skill.py`，须输出 SKILL_VALID。

---

## Task 0: 备份与基线

- [ ] **Step 1: robocopy 全量备份**（命令见 Global Constraints）
- [ ] **Step 2: 跑基线测试**，记录通过数，作为回归对照

---

## Task 1（问题④）: 明细/正表同文件双角色读取

**现状代码：**
- `src/cashflow_direct/pipeline.py:296-301`：文件被 `--statement-path` 指定且正表解析成功 → `continue`，该文件不再做明细扫描。
- `src/cashflow_direct/pipeline.py:362-363`：`if detected: continue` —— 文件识别出明细数据集后，不再尝试 `parse_existing_statement`。
- `src/cashflow_direct/pipeline.py:377-391`：未指定正表、且未识别出明细时，才尝试 `parse_existing_statement`；识别结果为多候选歧义时已有 `mapping_questions`（`kind='statement'`）提问机制（381-391 行），confirm-mapping 阶段作答。
- `src/cashflow_direct/statement.py:314-341` `parse_existing_statement(path, rules)`：按整个文件遍历全部工作表，只返回单一结果（唯一命中 → `ExistingStatementResult`（带 `sheet_name`）；多命中 → `MappingQuestion('statement_sheet', …)`；零命中 → MappingQuestion 或兜底）。单 sheet 解析在 `statement.py:147-311` `_parse_statement_rows`，返回 None 表示"该 sheet 不是正表"。
- `src/cashflow_direct/intake.py:113-115`：sha256 去重，同文件第二次选择被标 `duplicate_of` 并从 `active_files` 剔除。**本任务不改去重逻辑。**

**目标行为：**
1. 一个文件里有什么角色就读什么角色，互不排斥：
   - 识别出明细 → 照常读明细，**同时**继续对该文件**未被明细映射覆盖的 sheet** 尝试正表识别；
   - 被指定为正表的文件 → 正表解析成功后**继续**走 `scan_workbook`/`infer_dataset_mappings` 读明细页（把已确认为正表的 sheet 排除出明细推断）；
   - 自动扫描发现疑似正表页但用户未指定时 → 走既有 `mapping_questions`（`kind='statement'`）机制**提问确认**，不静默采用、不静默丢弃；
   - 指定了却找不到正表 → 维持现有报错（pipeline.py:302-304），不改。
2. 识别到疑似正表但未纳入核对时，**不允许** `overall_status="最终可使用"`，首页必须显著提示。

**Files:**
- Modify: `src/cashflow_direct/statement.py`
- Modify: `src/cashflow_direct/pipeline.py`
- Modify: `src/cashflow_direct/workbook_output.py`（首页提示，若状态文案需要）
- Test: `tests/test_statement.py`、`tests/test_pipeline.py`

- [ ] **Step 1: statement.py 新增按 sheet 的公开识别接口**

  新增函数（不改动既有 `parse_existing_statement` 的对外签名，避免破坏 test_statement.py 既有用例）：

  ```python
  def detect_statement_sheets(path: Path, rules: RulePack) -> dict[str, ExistingStatementResult | MappingQuestion | None]:
      """逐工作表识别正表。键为 sheet 名，None 表示该 sheet 不是正表。"""
  ```

  内部逐 sheet 调用 `_parse_statement_rows`。`parse_existing_statement` 改为基于该函数聚合（保持现有返回语义：唯一命中/多命中歧义/零命中）。

- [ ] **Step 2: pipeline.py 指定正表分支不再 continue**

  296-301 行：正表解析成功、设置 `existing_statement_path` 后，**去掉 continue**，继续执行 `scan_workbook` + `infer_dataset_mappings`；调用明细推断前，把正表命中 sheet 从候选中排除（做法：给 `infer_dataset_mappings` 加可选参数 `exclude_sheets: set[str] = frozenset()`，或在调用前过滤 `snapshot.sheets`——执行者二选一，以改动小者为准；`infer_dataset_mappings` 的既有调用方保持不变签名兼容）。

- [ ] **Step 3: pipeline.py 明细分支补正表识别**

  362-363 行：删除 `if detected: continue` 的短路语义，改为：无论 `detected` 是否非空，只要 `designated_target is None`（或本文件不是被指定文件），就对**未被明细映射覆盖的 sheet** 调 Step 1 的新接口：
  - 唯一命中且无 MappingQuestion → 追加一条 `mapping_questions`（`kind='statement'`，message 写明"检测到疑似正表工作表《X》，请确认是否作为客户现有正表核对"），由 confirm-mapping 确认后生效（确认机制复用现有 questions 流程；新增一种确认键格式，如 `{file_id}:statement:{sheet}`，取值 `use`/`ignore`）；
  - 多命中/歧义 → 维持既有 MappingQuestion 追加逻辑（377-391 行现有写法）；
  - 全部不是正表 → 什么都不做。

  注意：`existing_statement_path` 的设置时机从 preflight 内直接赋值，调整为"confirm-mapping 确认后写入运行状态"——检查 `confirm_mapping` 对应函数（pipeline.py 内，执行者自行定位）如何处理 `kind='statement'` 的既有回答，沿用同样路径。

- [ ] **Step 4: 疑似正表未核对时封锁"最终可使用"**

  `pipeline.py:1056-1063` 的 overall_status 计算增加一个条件：存在 `kind='statement'` 且未确认（或确认结果为 ignore 但确实识别到正表结构）的候选时，状态 = `待完成人工确认`（或新增的"草稿：存在未核对的疑似正表"文案，执行者选与现有风格一致者），并在首页（workbook_output.py:162-208 区域）显著提示。

- [ ] **Step 5: 测试**

  `tests/test_statement.py` 新增：`detect_statement_sheets` 逐 sheet 结果（正表页命中、明细页为 None、多正表歧义）。
  `tests/test_pipeline.py` 新增端到端：
  1. 合成"明细+正表同文件"工作簿，不带 statement-path → preflight 后 entries>0 且产生 statement 提问；确认 use 后 finalize 的核对报告非空；
  2. 同一文件带 `--statement-path` → entries>0（明细没丢）且 `existing_statement_path` 已设置；
  3. 疑似正表未确认 → overall_status 不是"最终可使用"。
  fixture 用 `tests/fixture_factory.py` 的工厂函数合成，不碰真实客户文件。

- [ ] **Step 6: 真实文件回归验证（只读）**

  对 `C:\Users\27651\Desktop\现金流量-清平.xlsx` 跑 preflight 两种场景，`--output-parent` 指向 `<root>\运行验证_<时间戳>\`（不污染桌面）：
  - 不带 statement-path：明细 2895 条 + 出现疑似正表提问；
  - 带 statement-path 指向本文件：明细 2895 条 + 正表已识别。

---

## Task 2（问题②）: 分类逻辑——标签不再免审

**现状代码：**
- `src/cashflow_direct/classification.py:124-142`：标签规范化后与标准项目全称一致 → 直接 return `EXACT-STANDARD-LABEL`，`evidence_level="high"`；144 行起的规则匹配完全不执行。
- 规则匹配 `_rule_matches`（classification.py:87-99）含方向过滤；规则字段仅 8 个（rule_id/item_id/priority/direction/summary_terms/account_terms/exclude_terms/evidence_level），priority 数字小者优先（FALLBACK=100 最低）。
- AI 任务筛选 `src/cashflow_direct/ai_review.py:71-93` `select_ai_tasks`：先排除 excluded、再排除 `abs(cash_delta_cent) < materiality.trivial_cent`；进入条件含 `not decision.resolved`、证据弱且达门槛、有 anomalies 等。
- `decision.resolved=False` 的事项在 finalize 进"重要待复核事项"（pipeline.py:932-977，经 `build_review_batches` 按 performance 重要性过滤）。

**目标行为：**
1. 标签命中标准名称时**不再直接放行**：照常跑 27 条规则独立分类，再比对：
   - 规则最高优先命中为高证据（high）且与标签项目**不同** → 记为冲突：保留标签项目作为系统首选，但 `matched_rule_id="LABEL-RULE-CONFLICT"`，`evidence_level` 降为 `medium`，`resolved=False`，`excluded_conflict_rule_ids` 记录冲突规则，reason 写明"原标签为X，规则Y高证据指向Z"；
   - 规则命中与标签一致，或规则无命中/仅低证据命中 → 采信标签，维持 `EXACT-STANDARD-LABEL`；
   - 冲突项送 AI **不受 performance 门槛限制，但仍受 trivial（明显微小错报临界值）门槛限制**（低于明显微小事项接受系统首选，与 SKILL.md 原则 3 一致）——在 `select_ai_tasks` 中为 `LABEL-RULE-CONFLICT` 增加专门进入条件。
2. 补充规则词条，让规则引擎对本次发现的错分类型有独立判断能力（改 `references/直接法分类规则.json`，新增/扩充规则的 item_id 必须存在于 `references/一般企业正表项目.json` 且 `is_leaf==true`）：
   - 新增 `CFO-07-PENALTY`（priority 6，outflow）：summary_terms=["滞纳金","罚款"], account_terms=["营业外支出"]，item_id=CFO-07；同时给 `CFO-06-TAX` 的 exclude_terms 加 "滞纳金"、"罚款"（防止税费规则抢）；
   - 扩充 `CFI-06-BUILD-ASSET`（priority 10）：account_terms 补 "长期待摊费用"（固定资产、在建工程若已在则不动）；
   - 新增 `CFI-02-STRUCTURED-DEPOSIT`（priority 18，inflow，须高于 CFO-03-BANK-INTEREST 的 19）：summary_terms=["结构性存款","理财"], item_id=CFI-02；同时 `CFO-03-BANK-INTEREST` 的 exclude_terms 补 "结构性存款"、"理财"；
   - `CFO-06-TAX` 的 summary_terms 补 "车船税"（覆盖随保险缴纳的车船使用税场景）；
   - 新增 `CFO-04-UTILITY-FUND`（priority 21，outflow）：summary_terms=["电费基金","电价附加"], item_id=CFO-04。
   - 以上词条以本次错分清单（Task 4 的规则表）为准逐一核对；执行者可补同义词，但不得删减。
3. 非现金事项识别（摘要写"计提"、应收票据背书抵账等）：在业务组成构建阶段（`src/cashflow_direct/components.py`，执行者自行定位 anomalies 赋值处）增加 `non_cash` anomaly 判定规则，使 classification.py:110-122 的 EXCLUDED 分支生效。判定依据至少两条：摘要含"计提"；同凭证内应收票据/应付票据腿与往来腿互抵且无货币资金科目。**保守原则：拿不准的不标 non_cash，宁可进 AI 复核。**
4. 红冲语义（SKILL.md 原则 6）不改合法性，只加体检：同凭证同时存在大额流入与流出却全部挂在流入项目（或反之）时，给 component 加 anomaly（如 `netting_suspect`），借 select_ai_tasks 的 anomalies 通道送 AI，不自动改分类。

**Files:**
- Modify: `src/cashflow_direct/classification.py`
- Modify: `src/cashflow_direct/ai_review.py`
- Modify: `src/cashflow_direct/components.py`（non_cash / netting_suspect anomaly）
- Modify: `references/直接法分类规则.json`
- Modify: `SKILL.md`（原则 3、6、9 改写，见 Task 5）
- Modify: `references/使用说明.md`（补一段"原标签如何被复核"的说明）
- Test: `tests/test_classification.py`、`tests/test_ai_and_materiality.py`、`tests/test_components.py`

- [ ] **Step 1: classification.py 重构 classify_component**

  124-142 的早退逻辑改为：先记录 `exact_item`，继续执行规则匹配；按"目标行为 1"的三分支产出 decision。保持函数签名与返回类型（ClassificationDecision）不变。注意 models.py:65-77 的字段均可承载新语义，无需改 models.py。

- [ ] **Step 2: ai_review.py select_ai_tasks 增冲突通道**

  在 trivial 门槛之后、既有进入条件中增加：`decision.matched_rule_id == "LABEL-RULE-CONFLICT"` 即入选（不再要求 evidence 弱或达 performance）。批次构造、脱敏、≤25/批逻辑不变。

- [ ] **Step 3: components.py 增加 non_cash 与 netting_suspect anomaly**

  按目标行为 3、4 实现，规则常量集中放在文件顶部并加中文注释。

- [ ] **Step 4: 规则 JSON 扩词**

  按目标行为 2 逐条改。改完检查 loader 硬约束（classification.py:79-83：规则引用的 item_id 必须存在）。

- [ ] **Step 5: 测试**

  - 标签与高证据规则冲突 → `resolved=False`、`matched_rule_id="LABEL-RULE-CONFLICT"`、入选 AI 任务（金额 ≥ trivial 即可，不需 ≥ performance）；
  - 标签与规则一致 / 规则无命中 → 维持 `EXACT-STANDARD-LABEL`；
  - 冲突金额 < trivial → 不送 AI、采信标签；
  - "计提"摘要、票据背书互抵凭证 → anomaly 含 non_cash、decision 为 EXCLUDED；
  - 新增词条单测：滞纳金→CFO-07、车船税→CFO-06、结构性存款利息→CFI-02、长期待摊费用→CFI-06、电费基金→CFO-04。

- [ ] **Step 6: 真实数据回归（只读）**

  用上次的运行输入重跑 classify 阶段（或写一段临时脚本直接对 2895 个 component 调 classify_component + select_ai_tasks），断言：
  - `EXACT-STANDARD-LABEL` 不再是唯一命中规则；
  - 1623 万滞纳金（记189）、社保滞纳金（6711）、厕所装修/食堂工程（长期待摊/在建工程）、结构性存款利息、电费基金、车船税等事项进入 AI 复核批次或冲突留痕；
  - 记231 票据背书凭证（5897 万）整体 EXCLUDED（non_cash）。

---

## Task 3（问题③）: 全量分类留痕 sheet 人机分层

**现状代码：** 留痕行字典在 `pipeline.py:983-1013`（内部划转排除行 1015-1037），列头由 `workbook_output.py:122-142` `_write_dict_rows` 动态取字典键序（134 行）。sheet 创建在 workbook_output.py:155。

**目标行为：**
1. 人读列在前：记录类型、摘要、现金变化、原现流项目、对方科目、**系统项目名称（中文，如"支付其他与经营活动有关的现金"，括号附编号 CFO-07）**、判断理由、证据强度、异常、方向依据、来源文件、来源工作表、来源单元格；
2. 机器列挪到最右并按顺序排列：决策来源、命中规则、业务组成编号、来源占用键；这四列表头上方或表头批注注明"技术追溯用，非审阅必需"（做法从简：表头文字改为"决策来源(技术)""命中规则(技术)""业务组成编号(技术)""来源占用键(技术)"，不做隐藏列，避免影响既有公式与打印区域逻辑）；
3. 不删任何列——复核与数据库回查能力必须保留。

**Files:**
- Modify: `src/cashflow_direct/pipeline.py`（trace 行字典：系统项目值改为"名称（编号）"、键序调整、四个技术列表头改名）
- Test: `tests/test_workbook_output.py`

- [ ] **Step 1: 改 trace 行字典**（键序即列序；系统项目值用 `f"{name}（{item_id}）"`）
- [ ] **Step 2: 测试**：断言列序与表头文案；既有"没有现金流业务组成。"空表路径不受影响。

---

## Task 4（问题①）: 错分清单导出交付物

**一次性脚本，不进 skill 的 src/。** 放 `<root>\scripts\export_misclass_20260815.py`（或桌面独立脚本，执行者任选，但必须在计划交付物中说明路径）。输入 `C:\Users\27651\Desktop\现流表明细.xlsx`（只读），输出 `C:\Users\27651\Desktop\现流表明细_错分标注.xlsx`：复制原"明细"页全部数据与格式（openpyxl 复制即可），右侧新增 4 列：`是否疑似错分`、`建议调整至`、`理由`、`涉及凭证`。

**判定规则表（按 2026-08-15 人工复核结论，逐行实现，摘要匹配用"包含"语义）：**

| # | 命中条件（科目/摘要/现流项目组合） | 现标注 | 建议调整至 | 理由 |
|---|---|---|---|---|
| 1 | 科目含"营业外支出_罚款、滞纳金"且项目="支付的各项税费"（记189-5：16,236,343.08；记266-2：19,045.60） | 支付的各项税费 | 支付其他与经营活动有关的现金 | 滞纳金/罚款不是税 |
| 2 | 科目含"营业外支出_罚款、滞纳金"且项目="支付给职工…"（社保补费滞纳金/利息，8 笔约 4.8 万） | 支付给职工 | 支付其他与经营活动有关的现金 | 同上 |
| 3 | 记231（2026/2/27）整凭证：2202 应付账款 +58,972,968.30 与 38 行 1121 应收票据负数 | 购建固定资产…支付的现金 | 不进现金流量表 | 票据背书属非现金交易；一正一负虽轧平但明细不该出现 |
| 4 | 摘要含"矿业公司"且含"电费"且科目为 5101 制造费用_电费或 2221 进项税且项目="收到其他与经营"（流量金额为负，约 -443 万） | 收到其他与经营（负数） | 购买商品、接受劳务支付的现金（正数付款） | 自家电费是付款，不得在收款项目内轧差 |
| 5 | 摘要含"收水电气、房租"且流量金额为负且项目="支付其他与经营"（约 -3.2 万） | 支付其他与经营（负数） | 收到其他与经营活动有关的现金（正数） | 收款挂到了付款项目 |
| 6 | 摘要含"收重庆中环工程款"（-372,466.67）、"付矿业公司850中段排水通道费用"（-80,000），项目="购买商品…" | 购买商品（负数） | 收到其他与经营活动有关的现金 | 收款/退回挂错方向 |
| 7 | 摘要含"茶叶费"且流量金额为负且项目="销售商品…收到的现金"（-2,250） | 销售商品收到的现金 | 收到其他与经营活动有关的现金 | 内部向部门收费不是对外销售 |
| 8 | 科目为 1604 在建工程（食堂设计费 47,883.50、多功能厅改造 22,601.95）项目="支付其他与经营"；科目 1801 长期待摊费用 3 笔（厕所装修 599,212.33→支付其他与经营；食堂建设工程 213,932.34+6,417.97→支付给职工；厨房设备 181,790→购买商品）；科目 1601 固定资产（食堂计费系统 43,350→购买商品）；摘要含"付总部食堂建设工程费用"且科目 1123 预付账款（-68,100→支付给职工） | 各自现标注 | 购建固定资产、无形资产和其他长期资产支付的现金 | 资本性支出，合计约 124 万 |
| 9 | 摘要含"结构性存款"（81,805.55、77,371.58、销项税 4,642.30）项目="收到其他与经营" | 收到其他与经营 | 取得投资收益收到的现金 | 结构性存款属投资活动 |
| 10 | 科目含"车船使用税"（2221 三笔约 5,151.30；6403 三笔约 4,836）项目="支付其他与经营" | 支付其他与经营 | 支付的各项税费 | 随保险缴纳的车船税是税款 |
| 11 | 摘要含"电费基金"（4 笔合计 81,876.56，科目 5001 生产成本）项目="支付的各项税费" | 支付的各项税费 | 购买商品、接受劳务支付的现金 | 电费基金是电费组成，不是税 |
| 12 | 摘要含"支付个人所得税"且科目 2241 待清理款项（18,015.28、17,554.86）项目="支付的各项税费" | 支付的各项税费 | 支付给职工以及为职工支付的现金 | 与记147（12,937.41 放"支付给职工"）口径不一致；代扣个税应随工资 |
| 13 | 摘要含"聚佳"（劳务外包管理费/社保/工资）且项目="支付给职工"（记105：216,891.58） | 支付给职工 | 购买商品、接受劳务支付的现金 | 付给劳务公司的派遣费不属"职工"；且摘要写"计提"，需人工核实是否实际付款 |
| 14 | 口径提示（不算错分，标注"口径不一致，建议统一"）：科目 2211 职工福利且摘要含"独生子女费/慰问金/护理费"（放"支付其他与经营"）vs 食堂食材（1411 放"支付给职工"）；科目 6603 手续费负数混入收款（微信扫码等小额轧差） | — | — | 提示人工统一口径 |

- [ ] **Step 1: 写脚本并运行**，输出文件后自查：总行数 2895（不含表头）；各类标注行数与上表笔数一致；随机抽 10 行人工核对"涉及凭证"与原表一致。
- [ ] **Step 2: 另输出一份汇总页**：按 14 类汇总笔数与金额合计，放在同一工作簿第二个 sheet"错分汇总"。

---

## Task 5: 文档与复盘更正

- [ ] **Step 1: SKILL.md** 改写三处：
  - 原则 9 改为："原现流项目与标准项目全称一致时，标签仅作平局兜底；规则引擎须独立分类，高证据规则与标签冲突的事项（达明显微小临界值）全部送 AI 复核"；
  - 原则 3 补一句："标签与规则冲突不受实际执行重要性门槛限制，仅受明显微小错报临界值限制"；
  - 原则 6 补一句："同凭证大额流入流出同挂一侧项目时标记异常送 AI，不自动改分类"；
  - 客户引导第 2 条与执行编排 preflight 条目：补充"同文件可同时识别明细与正表；自动发现疑似正表会提问确认；疑似正表未确认时不允许最终可使用"。
- [ ] **Step 2: references/使用说明.md** 补"原标签复核机制"与"同文件双角色"两段说明。
- [ ] **Step 3: 更正复盘文档** `C:\Users\27651\Desktop\现金流量表核对_问题复盘.md`（**先备份**到 `C:\Users\27651\Documents\Code\Backup\桌面复盘_20260815\` 再改）：把"责任划分"更正为——同文件双角色是工具缺陷（实测指定参数后明细为 0），操作者未传参数只是表象；"教训"一节改为"同文件场景在工具修复前需先把正表页另存为独立文件再指定"。
- [ ] **Step 4: 跑 `python scripts/validate_skill.py`**，须输出 SKILL_VALID。

---

## 验收标准（复核 agent 用）

1. 基线测试全绿 + 新增用例全绿（pytest 输出贴出，通过数 ≥ 基线 103 + 新增数）；
2. `validate_skill.py` 输出 SKILL_VALID；
3. 真实文件双场景 preflight 实测输出：不带参数 = 明细 2895 + 疑似正表提问；带参数 = 明细 2895 + 正表识别（两份运行状态 JSON 关键字段贴出）；
4. classify 回归：`EXACT-STANDARD-LABEL` 命中数 < 2895；AI 复核批次非空且包含验收清单事项（1623 万滞纳金、长期待摊三笔、结构性存款利息、电费基金、车船税）；记231 凭证整体 EXCLUDED；
5. 输出工作簿"全量分类留痕"：系统项目列含中文名；四个技术列在最右且表头带"(技术)"；
6. 桌面产出 `现流表明细_错分标注.xlsx`：2895 行、4 个新列、"错分汇总"页 14 类金额与计划规则表一致；
7. 复盘文档已更正且有备份；
8. 全程无对客户原始文件的写入（校验两个输入文件 SHA-256 前后一致）；
9. 备份目录存在且时间戳早于任何修改。

## 风险与边界

- 标签复核改动会改变既有输出（原来全照抄标签，现在会冒出 AI 批次与待复核事项）——这是**预期变化**，测试断言若依赖"全部 EXACT-STANDARD-LABEL"需同步更新；
- non_cash 判定宁漏勿错（保守原则见 Task 2 目标行为 3），漏掉的靠 AI 兜底；
- 执行 agent 不得顺手重构无关代码；任何超出本计划的改动单独列出并说明理由。
